# Deckhand — Project Handoff
(formerly "Classroom Clock")

## What this is
**Deckhand** (name chosen by Croix, Aug 2026 — the crew member who handles the deck; nautical, and it literally handles his Slides deck): a single-file HTML classroom tool (`Deckhand_v5.html`) built for Croix, a 7th grade math teacher at Windy Hill Middle School (Lake County Schools, FL — wolves, #OnePack; "HOWL Time" is their advisory block). It runs on his classroom machine, displayed on a **Promethean interactive touch panel**, alongside a presented Google Slides "Daily Math Board" deck. It exists because his school restricts Chrome extensions, ruling out the Slides Timer extension.

**Long-term vision (stated by Croix): a "classroom operating system"** — a family of live classroom tools in one file — while keeping every hard constraint below. It **complements** the Slides board, never absorbs it ("I can absolutely not compete with Google's own system"). OS scope is "things a slide can't be": live, ambient, or interactive. No static content, no daily-board duplication.

## Hard constraints — do not break these
1. **Single self-contained HTML file.** No build step, no separate CSS/JS at runtime (npm is for the TEST suite only). Runs by double-clicking on a locked-down school machine and from the Pages URL.
2. ~~No browser storage.~~ **RETIRED in v7.0** (Croix, Sep 25 2026): the board autosaves to localStorage (`deckhand.config`); the baked block is the seed; "Export a copy" is the backup/move path. Nothing ever leaves the machine.
3. **No external dependencies except Google Fonts** (DM Sans via `<link>`, `system-ui, sans-serif` fallback). Degrades gracefully offline.
4. **No extensions, no installs, no accounts.**
5. **Audio may be blocked** — the visual alarm must always work without sound.

## Design system (matches his "Daily Math Board" slides)
- Palette: cream `#F8F2E4` bg with 48px grid `#EADFC6`; sand `#E6D5B8`; hard-offset card shadow `#D8C5A0` (no blur); turquoise `#40E0D0`; pale turquoise `#D8F6F1`; deep teal `#127A85`; navy `#17324D`; coral `#FF7F6A` (**reserved exclusively for alerts** — alarm, low-time digits, SIM tag, settings errors); white cards.
- An "accelerated class" purple variant exists: main `#7E5FC5`, pale `#EDE6F9`, deep `#4B2E83`. **Purple theme toggle: shelved by Croix.** If revived: wave SVG fills are hardcoded hex → move to CSS vars first; coral stays the alert color.
- Type: DM Sans only, weights 400/500/700/900. All time digits `font-variant-numeric: tabular-nums`, weight 900.
- Motifs: navy rounded header bar; white rounded cards with colored header strips + hard drop shadows; layered turquoise/sand SVG waves at the bottom. Minimal motion; `prefers-reduced-motion` respected.

## Version scheme
- **v1** = original `Classroom_Clock.html` (clock + preset timer). Known-good fallback era; superseded.
- **v2** = Phase 1 (module architecture, custom entry, stopwatch, mute, hardened audio). **Accepted on the school machine** (touch + chime verified).
- **v3** = Phase 2 (config core, settings panel, download-copy, bell engine with week-parity automation). Superseded same day.
- **v4** = landing page + fully-manual schedule picking (parity automation axed as "wonky").
- **v5** = week groups: pick the week type once, Wednesday times apply automatically (Croix's design: "couldn't it simply ask if it is blue or black week and adjust the Wednesday schedule based on if it's Wednesday?").
- **v5.0.1 (Aug 30, 2026): the real 2026-27 bell schedule is baked in.** Weeks are **"Teal Week" and "Black Week"** (per the official 2026-27 sheet — not blue), 7th-grade lunch track, from Croix's PDF.
- **v5.1 (Aug 30, 2026) — `Deckhand_v5.1.html`**, per Croix's asks: **automatic week rotation is back** (anchor known: **week of Aug 31, 2026 = Black Week**) with the switcher in Settings, plus the **day sequence strip** ("which class next"). 30 automated tests green; focused adversarial review (4 findings, all fixed + regression-tested). Config `appVersion` "5.1.0", `schemaVersion` 3 (additive fields — older v5 configs load unchanged, rotation defaults off for them).
- **v5.2 (Aug 30, 2026) — `Deckhand_v5.2.html`** — readability + visual timers, per Croix: bell readout restructured with the **period as the hero** ("2nd Period" big navy 900; smart suffix — labels already containing "Period" aren't doubled; week name in a small uppercase pill; "Passing → 3rd" during passing); **"Bell countdown" toggle** (the ticking "39:00 until bell" line can be hidden if it distracts; period/bar/strip unaffected); **visual timer styles** — Settings "Timer style": Digits / **Ring** (default in Croix's config) / **Timer disc** (Time-Timer-style, auto-shrinking digits in the hole) / **Tide** (water tank that drains; very Deckhand). Visuals only in timer mode (entry/stopwatch fall back to digits); coral at ≤10s; +1:00 ratchets the visual denominator so the drain stays truthful. Settings close: pointer-close blurs (Space won't reopen the panel), keyboard-close restores focus. 40 automated tests green; adversarial review (4 findings: "Period Period" doubling, keyboard-close a11y, disc digit overflow, +1:00 pegged-full visual — all fixed + regression-tested).
- **v7.3.0 (Sep 26, 2026) = CURRENT — `Deckhand.html` (appVersion 7.3.0) — the lofi tape gets nine more sides.** Croix: "Can you expand my lofi tracks. The one that's there is excellent but I want some variation." The engine (`makeMusic`) keeps the v6.26 recipe — 32-bar sides of 8-bar phrases, recurring motifs, swung hats, the echo bus on the melody, master → tape lowpass → duck — and every SIDE now changes the room: `LO_TRACKS` = 10 entries `{name, key, bpm, prog, motif, voice, drums, hats, echo, tone, slide}`. VOICES (`keys(tr, …)` dispatches): `ep` (the original e-piano), `rhodes` (sine + fading 2nd partial + 4.2 Hz tremolo), `pluck` (dark sawtooth, fast filter pluck — kalimba/muted guitar; picked out over the 8ths on Kalimba, strummed on 1 and the and-of-2 on Bossa, and on the phrase's second half it answers the motif a third down), `pad` (two detuned triangles, slow attack). DRUM FEELS: `boombap` (the original), `halftime` (one big snare on 3), `brush` (no kick, brushed snare swishes + shaker 8ths), `bossa` (four kicks, a clave-ish `rim` click), `none`. `bass(…, slide)` scoops into the note on the slide sides. `LO_PROGS` grew to 7 (turnaround, four-chord, borrowed iv, minor-leaning), `LO_MOTIFS` to 8. The sides: Porch swing (side A — the original, untouched), Rainy window, Late bus, Library (the v6 four), Glass rhodes, Kalimba, Brushes, Bossa, Fog (pad, no drums, the sleepy one), Corner store. ROTATION: `nextSide()` — the first pass goes A→B→C… in order, then shuffles, never repeating the side that just played; tone and echo glide to the new side's values (`setTargetAtTime`). **Next ›** chip (`.muNext`, `skip()`, a PLAY action live while locked): every voice plays into a SIDE BUS (`bus`) under `master`; `newBus()` fades the old bus out in ~80 ms under the new side and disconnects it after 1.5 s — a cut, not a mush; the echo tail carries over (it feeds `master` directly). Before Play, Next just turns the tape over (no AudioContext). `sideName()` = "Lofi — <name>" in `.muWhat`. Loudness checked headless with an analyser tap: kicks peak the same on every side, no side is silent. TEST: "v7.3 music: ten named sides…" in `tests/settle-and-media.spec.js` (188 tests, all green at 2 workers). Two stale/flaky tests fixed on the way: the stage-bar count is 3 since v7.2 added Draw (`stage-and-sea`), and the suite's keep-the-store flag now rides in `window.name` as well as sessionStorage — a sessionStorage write raced `page.reload()` on file:// about one run in six and the fresh document cleared the store ("rosters lost", "legacy note not migrated" — test infra, never the app).
- **v7.2.0 (Sep 26, 2026) — `Deckhand.html` (appVersion 7.2.0) — the pennant, the Ink, the Pledge take two.** Croix, same day: "add in an animation of the sailboat flying a Mr. Shaffer Rules flag every now and then… make sure the flag and crap doesn't fly for period one when they are on the reversed schedule… I'd like the settle in timer to be only 25 seconds next week, but only for 1st period at the end of the day. I'd like to be able to adjust that as I please… I don't want the timer and text, just the flag for 2 minutes alone on screen… subtly wave… draw over anything via the tool pad in some elegant way." **(1) Pledge = FLAG ALONE**: `start(auto, firstBell)` on the first bell no longer starts the count — `t.endAt = null`, `pledgeUntil` opens, `tell("start")` stages the settle, `paint()` blanks top/big/sub and `.stPledgeOnly` hides the chips/hint/✎ row; `pledgeOver()` → `tell("end")` → the slides. `firstBell` = `s.idx === 0` = the day's FIRST BLOCK, whatever its label — so Black week flies it for 6th at 9:20 and NOT for 1st at 3:07 (test: "on a reversed (Black) week…"). `.stStage` is now `width:100%; align-self:stretch` (it shrank to 300px around the flag). **Wave** = a one-time `feTurbulence`+`feDisplacementMap` ripple baked into `FLAG_SVG` (`#flagRipple`, scale 26 — NO per-frame turbulence, a Chromebox can't afford it full-screen) plus CSS `flagSway` (perspective rotateY ±3.5° / skewY) and a `flagSheen` gradient sweep on `.flagWave`, both under `body.waveMotion`. **(2) Per-period seconds**: `wcfg.perPeriod = {label: secs}` (≤12, 5–300, sanitized); `secsFor(label)` (override → default) feeds `start()` for the CURRENT period and the armed "25s" readout (a 5s repaint follows the bells); `.stPer` grid in the ✎ row = one number box per `bellPeriodLabels()` (Lunch excluded) + any orphaned override, blank = delete. Next week (Black) 1st = 25 is a single box. **(3) The pennant**: `.btPen` group INSIDE `#boat` (drift + sway come free; `#boat{overflow:visible}`) — mast line + `.btPenCloth` (navy, turquoise 14px text "<ownerName> Rules", set at play time; a static `#penRipple`); Sea THING "pennant" (DUR 22s, WEIGHT .6) whose element is the boat and class is `pennant` (`elFor/clsFor`); `penRun` scales the cloth from the mast, waves (skewY ±2°) and furls; `mastUp` raises the mast. Coral was tried and rejected — it is the alert color. **(4) INK** (`var Ink`): `#ink` fixed canvas z 14 (over widgets and the stage's z-900000 bar, which lives inside the canvas's z-1 context; under the dock 15 / settings 20 / alarm 30), `pointer-events:none` unless `body.inking`; `#inkBar` z 16 bottom-right (6 swatches incl. a yellow HIGHLIGHTER drawn `multiply` at .45 alpha, 3 sizes, Eraser = `destination-out` at 3× width, Undo, Clear, Done); armed by `#inkBtn` (dock, after Lock — live while locked, a teaching action), `#inkBtnStage` (focus bar, for staged decks) or **D**; Escape while armed = Done; Done KEEPS the ink (annotate, then keep clicking the deck); Home disarms; strokes are CSS-pixel point lists smoothed with quadratic midpoints, re-rendered on resize at DPR ≤2; one pointer at a time (a second finger is ignored); session-only; hidden on phone. `Deckhand.ink` = test hook (`count/clear/arm/isArmed/toggle`). TESTS: `tests/v72.spec.js` (pennant incl. name follow, Ink incl. a CDP touch stroke @touch), v71 Pledge tests rewritten (flag alone; Black-week 1st gets 25s and no flag; blank box clears the override).
- **v7.1.0 (Sep 26, 2026) — `Deckhand.html` (appVersion 7.1.0) — the week-of-notes release.** Croix's notebook from the first week on 7.0 (photographed, Sep 21–25) plus his answers to four clarifying questions. **(1) TODAY override** — his note "Wednesday schedule on non-Wednesdays" was a WANT, not a bug: `cfg.bell.today = {date, mode: regular|wednesday|custom, blocks[]}` (dated: expires by itself) + `cfg.bell.customLast`. `Bell.overrideFor(day)`/`dayBlocksFor` honor it; `statusAt` runs a custom day even with No bells (-1) — that IS the use case (testing days). Home screen TODAY row `#todayChips`; `Bell.setToday(mode, blocks)`, `Bell.todayMode()`. **`BlockEditor(container)`** (rows: label + `<input type=time>` ×2 + ✕; `fill/read/add/shift/isEmpty`; school-convention 6:00 AM–5:59 PM enforced; ≤20) is the touch editor Croix wanted instead of a text box ("smoother experience") — used by the **`DayEditor`** sheet (`#dayEditorWrap`, Start-from buttons, ±5 min shift) AND by Settings' Schedules tab (per week type: two editors + an "Edit as text" toggle that reveals the original `taGrpRegN/WedN` textareas; `apply()` reads editors unless that card is in text mode). **(2) Rich NOTE** (Croix: "bold underline change text color, font… especially size — my students couldn't even see it"): `wcfg.html` (≤20k) + `wcfg.size` auto|s|m|l|xl; contentEditable + `.txBar` toolbar (execCommand with styleWithCSS; fontSize via the `<font size=7>` trick → exact em span); base type `clamp(20px, 9cqmin, 120px)` × `--tx-scale`; Fit shrinks to 0.5 only in auto — an explicit size never shrinks; spawns 40×60. **`sanitizeNoteHTML()`** (DOMParser, tag allowlist, drop-list with contents, style allowlist: weight/italic/underline/color/font-size→em 0.5–4/font-family→4 stacks/text-align) runs in `sanitize()` AND on every commit AND before every render; `legacyTextToHTML()` converts pre-7.1 `text` once. Base font-weight is 500 (700 made the browser's Bold toggle UN-bold). Paste is plain text. **(3) Noise game**: `wcfg.strikeSecs` ½/1/2/3 (chips, default 1), strikes REPEAT every 2×interval while over (a continuous roar used to cost one), `wcfg.tally = {date, byPeriod}` shown as "Today: 2nd 3 · 4th 1", dated so it wipes itself. **(4) Slides card**: `.embBigIn` + Load in the empty state (the small row now only opens with ✎); `wcfg.saved` ≤8 `{name,url}` library — ☆ saves the showing deck (prompt for a name), chips load (PLAY action), ✕ in the ✎ row removes; **`wcfg.stageOnly`** (embed + yt) = the SHELF: `.wShelf` hides the card on the board, `#shelf` dock chips ("▶ Slides") stage it, Exit re-shelves, and the settle-in `end` handoff finds it as before — this replaces Croix's "wonky flow" (drag the settle, open the embed, hide it behind the clock, tap, tap); a card being edited (`.embEditing`) never vanishes under the finger — Stage only takes effect when the ✎ row closes. `renderSaved` rebuilds only on a signature change (rebuilding under a tap detached the chip mid-click — same mechanism as the old scoreboard-rename bug). Iframes: `allow="fullscreen *; autoplay *; encrypted-media *; picture-in-picture *"` REPLACES `allowfullscreen` (which delegates to the frame's own origin only — a YouTube player inside a Slides deck is a third origin); verified in a nested file/http fixture (`document.fullscreenEnabled === true` two frames deep); UNVERIFIED on a real Google deck — that's a field check. **(5) The Pledge** (Croix: "Don't ring first period of the day for Pledge (show American flag)… keep the flag for like 2 minutes then transition into the embedded slides"): `wcfg.pledge = {on:true, minutes:2}` on the settle widget; `bellWatch` passes `s.idx === 0` (the day's first block) into `start(auto, firstBell)`; `pledgeUntil` opens the flag (`FLAG_SVG`, drawn inline: 13 stripes, 50-star canton) above the count; after the done spell the routine HOLDS in a `stPledgeOnly` state ("Pledge & announcements / m:ss / then the slides") and `pledgeOver()` fires the `end` handoff; manual restarts and R drop it; ✎ row has the checkbox + minutes. **(6) The sea** (Croix: "Still doesn't sway… kids loved the sea monster. Expand. Make it attack ship. Make it smoother."): the boat's paths moved into `.btRig` with `boatSway` 3.2s ±6° (the 45s drift was 1.4px/s — invisible); NEW `#sea-turtle` (40s paddle), `#sea-school` (15s darting bursts of five), `#sea-bottle` (51s, "x" on the note), and **`#sea-attack`** (9s: rises behind the hull, the neck strikes DOWN onto the sails twice — positive rotation only, anything that rears above the wave band hides behind the widget cards at z 1 — and `#boat.rocked` runs `boatLurch` at 36%); `WEIGHT.attack = 0.35` from the scheduler, ×3 from a boat tap (`pickOne("attack")`); `#sea-attack svg{overflow:visible}`; `Sea.clear()` also clears the rock. **(7) Design pass** (Croix: "sloppily designed… especially the settings"): Settings → tab rail (`#setRail`, `Settings.showTab/currentTab/textMode`, exposed as `window.Deckhand.settings`), `.fGrid/.fField/.fLabel/.fHint/.fCheck` field system, 48px controls, fixed footer `#setFoot` (errors above the single action row), new Device tab (version, last saved, source, Export vs Reset copy, privacy note); the `secX.open = true` idiom is a compatibility shim that selects the tab (and text mode for Schedules). + Add → `.pop` tile grid with 19 new `<symbol>` icons + descriptions (3 columns, 4 at ≤1100px tall, 5 at ≤820); scene manager and ⏱ menu are `.pop` sheets. **TESTS**: `tests/v71.spec.js` (today/day editor, noise tally, embed paste/library/shelf/routine, nested fullscreen @http, Pledge ×2 (one has a real 1-minute window — `test.setTimeout(150000)`), sea); text-box tests rewritten for rich notes; settings tests rewritten for tabs (`dh.tab(id)`, `dh.openScheds()` = scheds tab + text mode ×4). Known flake in this sandbox: the v6.26 YouTube widget test under a 3-worker load (passes alone).
- **v7.0.0 (Sep 26, 2026) — `Deckhand.html` (appVersion 7.0.0; the file is VERSION-FREE from here on — `Deckhand_v6.html` is a forwarder, Export writes `Deckhand.html`, the header badge reads cfg.appVersion) — the board saves ITSELF; the September review lands.** A full top-to-bottom review (architecture, security/privacy, tests, CSS/a11y/UX, repo) found no XSS and a clean public repo, and a list of real bugs + one strategic contradiction: GitHub Pages fixed YouTube Error 153 (https referrer) but the ONLY persistence was Download-copy → a `file://` copy → Error 153 again. Croix's answer: his other tools (Seating Chart, Cadence) keep rosters in the browser and they survive restarts on his devices — "that's what I want from Deckhand". So the "no browser storage" hard constraint is RETIRED. **(1) `Store` + autosave**: `localStorage["deckhand.config"]` = `{v, savedAt, fileVersion, cfg}`; the device copy WINS at boot over the baked block (which is now the seed / factory reset); `SaveState` diffs every 2.5s and writes on change, plus `pagehide`/hidden flush; the coral dot means only "a save FAILED — export"; `beforeunload` prompts only then. `cfgSource` ∈ file|device|defaults (`Deckhand.configSource`); a corrupt device copy is ignored (file boots), a broken FILE block with a good device copy runs on the device copy but export refuses (`fileBroken`). **(2) Settings**: "Download configured copy" → **Export a copy** (same filename, same serializer); **Use this file's settings** (two-tap, coral-armed, `Store.clear()` + reload); `#storeNote` status line; the action row is STICKY (it sat below the fold at 1080p — the reviewer's screenshot). **(3) Update nudge** `#verNudge`: a newer release opened over an older device copy says so once and offers "Use the new bell schedule" (adopts the file's `bell.groups/anchor/defaultGroup` only — rosters and layout stay); the device copy re-stamps `fileVersion` either way. **(4) Rosters from the Seating Chart**: `Import from Seating Chart` reads `localStorage["seatingchart.v1"]` (same-origin: both tools opened the same way on one machine) or `Import a Seating Chart backup…` (its JSON export); `{periods[{id,name:"Period N"}], students[{periodId,first,last,nick,active}]}` → names by period NUMBER into the roster boxes (nick||first, inactive skipped, unmatched periods skipped); Apply commits. **(5) Review fixes**: unknown `schemaVersion` → the corrupt banner + export off (was a silent factory reset with download LIVE — the exact data-loss path the banner exists for); `#t=%` no longer throws inside the top-level IIFE (blank board); the yt card listens for `deckhand:home/board` like Slides; a manual week-chip override EXPIRES at the date change for week types 0/1 (No-bells and types 2+ survive) — the Settings copy already said "for the day"; `buildDynamic` wraps each factory (a `.wBroken` card, the rest of the scene mounts); `parseTemplateText` refuses a 21st block at Apply (sanitize keeps 20 on reload) and the meter never writes a 13th record (`hasOwn` helper — bare `obj[k]` reads on "constructor"-style labels are gone in that path); hold-to-unlock resets `held` at pointerdown (touch fires pointerleave BEFORE click, so that was the only safe place); `embedURL` rejects youtube.com channel/@/user/results pages ("" — never the raw unframeable page) and accepts youtube-nocookie only as the embed player shape. **(6) Design**: contrast pass — `#bellWrap.warn #bellSub`, `.stBig.low`, `.stMsgB`, `#setErrors` are navy-on-coral; `.stBig.stArmed` teal; the selected-widget outline is TEAL (turquoise-on-white was 1.64:1); `.pill`/`#dock .pill` ≥40px; dead `#clockCard/#timerCard/#timerBody/#tideWaves` rules gone; the last off-palette literals are tokens (`--turq-light/--turq-mid/--coral-deep/--coral-pale`; wave fills via `.wvSand/.wvLight/.wvMid`); a11y: dynamic widgets carry `aria-label` + "Remove <name>", iframes have titles, `#dock` is a `<nav>`, popovers are `role="group"`, `#bellSub` is `aria-live`. **(7) Repo**: suite ported to `@playwright/test` — `tests/*.spec.js` by feature, fresh context per test, 4 workers, traces on failure, `expect.poll` instead of ~145 sleeps, projects `file` / `touch` (real CDP touch drag) / `http` (tools/serve.js, a Pages-like origin); `tests/helpers.js` clears the device store on every navigation so `openAt` still means "fresh boot from the file" (`dh.reopen` keeps it). GOTCHA: `reducedMotion` is NOT a top-level `use` option in @playwright/test — it must ride `contextOptions` (three porters hit it independently). `tools/check.sh` refuses any committed html whose config carries rosters/URLs/a non-default owner; `push.sh` runs it and keeps the token in `GIT_CONFIG_*` env, not argv. `.github/workflows/test.yml` (guard + 3 projects). Old versions → `archive/`, screenshots → `screenshots/`, this file → `docs/HANDOFF.md`; LICENSE (MIT), CHANGELOG.md, `.nojekyll`, playwright declared. **FIELD FACT (Sep 26, Croix): github.io is BLOCKED on the classroom Chromebox** — the Pages copy never reaches the panel, so the Error-153 fix it carried is moot at school; the Drive file over file:// stays the daily driver and ↗ Tab is the YouTube answer there. His school account cannot run Apps Script, but a web app DEPLOYED FROM HIS PERSONAL ACCOUNT (Execute as Me / Anyone) is reachable from the school account (that pattern already works for his shared files) — `hosting/apps-script/` holds Code.gs + a README with the four sandbox unknowns to verify on the panel (storage persistence across reload, YouTube in-tile, fullscreen, downloads). `FS.init` now hides the Fullscreen pill when `document.fullscreenEnabled === false` (a frame without allowfullscreen). **STANDING RULES**: all three PATs pasted into chat during this session are burned — rotate; the public copy rule is now enforced by check.sh, not prose.
- **v6.33 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.33.0) — the library grows up; the roster privacy rail.** Croix, now that the board is PUBLIC: "Ive gotta be careful now because this is online. So no student names. On the YouTube widget, can you expand the offerings" (Math Antics sorted to his content, curated TED, more ambience, instrumental Disney/video-game, school-safe pop/hip-hop/jazz). **(1) YT_LIB: 5 categories/17 entries → 8/39**, sourcing rule kept (every url live-searched Sep 20, 2026, never guessed): NEW **"Math Antics"** leads the library — 7 OFFICIAL channel playlists (their `PLUPEBWbAHUs…` set): Fractions, Percents, Ratios & proportions, Integers, Algebra basics 1+2, Geometry (no official Statistics playlist exists — searched, absent, don't guess one); NEW **"Movie & game scores"** (Disney piano ×2, Zelda for studying, Minecraft C418 ×2); NEW **"Pop, jazz & beats"** (clean classroom pop instrumentals ×2, pop study mix, Lofi Girl's jazz lofi radio); **Ambience** +4 (ocean 10hr, thunder & ocean, birdsong & forest, brown noise); **Watch & learn** +2 (TED-Ed "Math in Real Life" — note its LEGACY 18-char list id `PLE3376FF44087B17B` is valid — and "Think Like A Coder"). No UI changes: the overlay scrolls, and reviewer verified the last pill picks correctly at small widget sizes. **(2) Privacy rail**: the Settings roster note now warns in bold that a configured copy with rosters carries student names — keep those copies in Drive, NEVER upload one to the public site. Shipped default config re-verified clean (rosters `[]`, "Mr. Shaffer" the only name) and its stale `appVersion` re-stamped (reviewer LOW: the JSON block still said 6.29.0; version bumps must touch BOTH the JS DEFAULTS and the `#deckhand-config` block). **STANDING RULE for the public era: before ANY push, confirm the baked config block carries no rosters/decks/names beyond "Mr. Shaffer" — pushes deploy.** New suite test: 'v6.33: every library pill is a distinct, valid nocookie embed' clicks ALL 39 pills through the real setURL gate (Set-based dupe check). Reviewer delta probe: the one LOW above, fixed; data audit 39/39 clean. **152 tests green.**
- **v6.32 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.32.0) — ticks start at 10; the iframe DRAG SHIELD; buried yt handles unburied.** Croix: "make the clock only audibly start ticking at 10 then keep the fast sounding ticks at 5." **(1) TICK_AT=10**: the settle count is SILENT until 10s remain; inside the window the swell is remS-based (`fEl = 1 - remS/10`, lvl 0.10→0.79) so a 15s and a 5-minute settle sound IDENTICAL; half-beats unchanged at ≤5s. `t.total` removed (nothing reads it — mid-run ✎ edits can't touch the tick since wcfg.seconds left the tick path). Boundaries reviewer-verified: 10s and 11s runs tick first at remS=10; restart mid-window goes silent again until its own remS=10; no double-fire, no stale half-beat. **(2) THE BIG ONE — iframe drag shield.** The intermittent '⤡ grip did not resize' suite failure was NOT a flake: with a LOADED cross-origin embed (YouTube), the browser routes pointer input to the out-of-process iframe by hit test, so any drag freezes the moment the pointer crosses the frame EVEN THOUGH startPointer holds setPointerCapture. It only ever passed here when the egress proxy KILLED the YouTube load (ERR_TUNNEL keeps the frame in-process) — and it is why "stretching tiles" still died on Croix's tablet over a live video after v6.28. FIX: `body.dragging iframe{pointer-events:none;}` + body class add in startPointer / remove in teardown (the single exit shared by up/pointercancel/lostpointercapture, so it can't stick — reviewer verified ✕-mid-drag, scene-switch-mid-drag, lock-mid-drag, second-finger). Every drag surface funnels through startPointer, so strip moves and all handles get the shield too. Bisect scripts: scratchpad dbg_grip*.js. **(3) Reviewer MED found during the shield probe, fixed:** since v6.27, `.w-yt .wBody{position:relative}` (the Library anchor) buried ALL FIVE classic resize handles on the YouTube card (later positioned sibling paints over z-auto handles) — only the grip could resize it. FIX: `.wHandle{z-index:3}`. Regression tests: mid-drag shield up (body class + computed pointer-events:none) and down after drop; hSE shrink commits on the yt card. TEST NOTE: 'v6.29 fix' became 'v6.32: ticks hold their tongue until 10'; the grip section also asserts x/y unchanged (mis-aim tripwire). **151 tests green — twice consecutively, plus a third run with the handle fix.** **HOSTED (Sep 20, 2026):** the project is backed up at **github.com/croix18/Deckhand** (`tools/push.sh` commits + pushes with the preemptive Basic-auth header the sandbox proxy requires — token in git-ignored `.github-token`, per Croix's GITHUB_FROM_A_SESSION.md notes) and Croix enabled GitHub Pages (main /root) by hand, so Deckhand is LIVE at **https://croix18.github.io/Deckhand/** (index.html forwards to Deckhand_v6.html; verified serving). Every push to main redeploys — `tools/push.sh` IS the deploy, so pushes to main are now a PUBLISH: only push a release that passed the full ritual. The Pages URL sends a real referrer, so YouTube EMBEDS should now play in-tile from the panel bookmark (Error 153 was the no-referrer file:// refusal) — CONFIRM ON THE PANEL next field day; ↗ Tab stays as the fallback and the file:// double-click flow still works unchanged.
- **v6.31 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.31.0) — the done note becomes a 3-note phrase; the count says "seconds".** Croix on v6.30: "make it 3 notes but in that same kinda tone/vibe. Also the text says find your seat number to be in your seat. That could be confusing." **(1) `playSoft()` is now a rising C-major arpeggio** — roots 523.25/659.25/783.99 Hz (C5-E5-G5), 220ms apart, each note keeping the v6.30 recipe exactly (partials ×1/×2/×3 at gains 1/.35/.12, 20ms attack, 1.4s exponential fade). Probe: 9 oscillators per phrase, peak 0.40, silent by ~1.8s. `soft()`'s gate and single call-per-zero unchanged. **(2) Mid-count copy fix**: the running sub line was still "to be in your seat", which under "Find your seat / 3" read like a seat NUMBER — it now says **"seconds"** (singular "second" at 1). Armed and re-armed keep "to be in your seat"; done sub stays empty. Reviewer delta probe: **zero findings** (no "0 seconds" frame reachable from the loop — it flips to done first, a sub-200ms external-repaint flash is theoretical-only; strikes scheduled on the shared ctx finish cleanly ~1.4s past a mid-phrase reset/✕ destroy BY DESIGN, no leak; volume-0 ramp throw swallowed as with chime; double-zero = 38 oscillators exactly, silent after; no stale copy in product or suite). **151 tests green.**
- **v6.30 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.30.0) — Hamilton is OUT; zero gets one soft note.** Croix, same day as v6.29: "remove Hamilton stuff I changed my mind. I do like the audible countdown. The times up can you make it one pleasant sound instead of the alarms up sound?" **(1) The quote ladder is fully deleted** — QUOTES/setQuote/lastQuote, `.stSub.stQuote` CSS, `.qPop` rule + `qPop` keyframes, all gone; the sub line stays "to be in your seat" through the whole count, the done header is plain **"Time's up"** (sub cleared during the spell, restored on re-arm). The **tick swell stays exactly as built** (0.10+0.85·f², half-beats ≤5s, `t.total` frozen at start — the v6.29 review fix now protects only the swell, which is all that reads f). **(2) `Sound.soft()`** — the settle's zero now plays ONE bell strike instead of the 3-note `chime()`: sine partials **523.25/1046.5/1569.75 Hz at ×1/×0.35/×0.12 volume**, 20ms attack, exponential fade dead by 1.5s (analyser-verified: peak 0.27, monotone decay, 3 oscillators, no tail). Same on/ctx gate as chime; exported on the Sound object (rides the `Deckhand.sound` hook). `chime()` still serves its OTHER callers — the timer alarm ring and the bell warn — reviewer-verified untouched. Reviewer delta probe: **zero findings** (no quote residue in CSS/JS/tests, no sub flash at the done→armed repaint, suspended-at-zero = one possibly-late note never a doubled one, volume-0 throw swallowed like chime's, overlapping strikes leak nothing). TEST NOTE: the v6.29 feature test became 'v6.29/30: ticks swell to zero, one SOFT note lands, no lyrics anywhere' (asserts soft===1 && chime===0 at zero and that the sub line NEVER changes mid-count — the regression guard against lyrics creeping back). **151 tests green.**
- **v6.29 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.29.0) — the settle-in IS the default board; the count ticks louder as it dies; Hamilton flashes onscreen.** Croix: "make the settle in timer the default with the classroom clock. Leave off the timer. … as the clock gets closer to zero the louder the seconds … gets. Also, Hamilton quotes flash onscreen. I'm running out of time and my times up... Etc synchronized with the settle in timer." **(1) DEFAULT_SCENES**: Daily Board = clock {0,0,58,100} + **settle {61,0,39,100, label "Settle-in"}** — the default timer is GONE (Stations scene untouched). Timers still arrive via +Add and the ⏱ stage menu; the FIRST timer built still gets the legacy `#timer` ids. Old downloaded configs are untouched (explicit `scenes` win; only a scenes-less config gets the new default; v3→4 migration lands on it too — verified by the reviewer). **(2) The swelling tick** — `Sound.tick(level)` (square 1100→700Hz through a bandpass, ~50ms, level×volume, gated by Sound On + non-suspended ctx; exposed as `window.Deckhand.sound`, a test hook). makeSettle fires one tick per second at `lvl = 0.10 + 0.85·f²` (f = elapsed fraction — whisper → knock), plus a **half-beat at lvl×0.55, +500ms, inside the last 5s** (seconds becoming almost-milliseconds). All cleared at zero/reset/destroy — no tick plays armed, in the done spell, or after reset. **(3) The QUOTES ladder** (short uncopyrightable phrases only), paced by fraction so any length keeps the arc: ≤0.22 "Look around, look around…" → ≤0.45 "Talk less. Smile more." → ≤0.65 "History has its eyes on you." → ≤0.85 "I'm running out of time…" → "Just you wait…"; at zero the top line is **"My time's up!"** over the consequence message. `setQuote` toggles `.stSub.stQuote` (italic teal, huge in `.wFull`) and restarts a `.qPop` entrance (keyframes scoped to no-preference; reduced-motion swaps instantly). **REVIEW FIX**: the run's length is **frozen at start (`t.total`)** — paint's f and the tick's lvl read it, NOT live `wcfg.seconds`, so a ✎ mid-run edit can't warp the arc against the visible number (reviewer's one finding, LOW; regression-tested). Reviewer delta probe otherwise clean: old-config compat, tick gating/leak/volume-0, quote ladder order + re-arm, bell routine fullscreen→embed handoff with the default settle, S-cycle/close-recovery/locked ⏱ menu on the new board. KNOWN + accepted: settle ticks keep clicking through a timer-alarm ring (layering, by design); `Deckhand.sound` is devtools-only surface. **TEST NOTES**: `addTimer()` helper (⏱ menu → [data-custom], parked at 61,0,39,100) replaces the old default-timer assumption in ~35 tests; dirty-flag asserts that addTimer would spend became JSON config-snapshot comparisons; S-cycle includes the settle; close-recovery may hand selection to it. **151 tests green.**
- **v6.28 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.28.0) — the ⤡ grip and the ↗ Tab escape, from Croix's tablet field test.** His screenshot showed TWO failures. **(1) YouTube "Error 153"**: YouTube now refuses EMBEDDED playback when the page sends no HTTP referer — and a double-clicked local file (or a tablet's content:// Downloads view, as in his screenshot) sends none. There is NO clean workaround from file://; the honest fix is **↗ Tab** (`.ytOpen` chip after Library): `watchURL()` converts the loaded nocookie embed back to the real `youtube.com` watch/playlist URL and opens it in a new tab — audio keeps playing in the background tab while Deckhand keeps the board; the empty-state text explains exactly this. Popup-blocked `window.open` returns null harmlessly. WHETHER the school panel's file:// embeds also 153 is UNKNOWN until his next field day — if they do, ↗ Tab is the standing answer. **(2) "Stretching tiles" failed on touch** — NOT the missing-touch-action bug (that CSS was already there): the resize handles are 14px edges / 28px corners, fine for the panel's precise IR pointer, hopeless for a finger — and on iframe widgets every miss dies inside the frame. FAILED FIX, documented so nobody retries it: fattening the handles inward — measured bottom-row chips (Eraser/✎/Start) center just **16–23px above the widget edge**, so finger-depth zones cover them; three suite clicks died proving it (the CSS comment above `.wHandle` carries the numbers). THE FIX: a **⤡ grip in the strip** (`.wSize`, a DIV — `startPointer` early-returns on `button`s — before ⛶, in `buildDynamic` AND the static clock markup; new `i-resize` symbol; 44px via the shared strip-icon inflation; `touch-action:none`; pointerdown → `stopPropagation` (or the strip ALSO starts a move) + `startPointer(…, "resize", "se")` — the resize math is DELTA-based (`R = ox+ow+dx`), so dragging from the top strip grows the right/bottom edges 1:1). Hidden while locked and on `.wFull`; aria-hidden, not tabbable. Reviewer delta probe: **zero findings** (no wedged drags across alternating grip/strip spam, clock config writes land, clamps/min sizes hold, lock-mid-drag discards, ghost keeps it unreachable, grip-resize of a summoned stage-boxed timer ADOPTS at 2dp per the pend rule, ↗ Tab conversions exact incl. UU-uploads lists). TEST NOTE: the v6.13 strip-icon count is now **4**. **149 tests green.**
- **v6.27 (Sep 20, 2026) — `Deckhand_v6.html` (appVersion 6.27.0) — the YouTube LIBRARY.** Croix: "give me a decent amount of YouTube options organized by category" (+ "I also love the mood melodies channel"). A static `YT_LIB` constant — **5 categories, 17 entries: Study music** (Lofi Girl radio, **Mood Melodies** — rides its channel-uploads playlist `UU R68jfDGAqtZM15vgb3epDg`, always opens on the newest upload, because guessing a video id was off the table; Chillhop radio, Coffee shop radio), **Classical** (4 HalidonMusic playlists incl. Best of Mozart), **Ambience** (two rains + a fireplace), **Brain breaks** (Koo Koo Kanga Roo ×3), **Watch & learn** (TED-Ed riddles, Veritasium maths, Numberphile). **EVERY url was researched via live web search on Sep 19-20 2026, none guessed** — the sourcing rule for any future additions. UI: a "⊞ Library" chip that `renderStations()` prepends (same node across re-renders, listener persists); it toggles `.ytLib` — an absolute navy overlay over the video area (menuLabel sections, pill per entry, × Close, overflow-y for small boxes); a pick = setURL + close (PLAY action, works LOCKED — no body.locked rule touches the panel); ☆ Save promotes any pick to a personal station (all library urls pass the nocookie gate trivially). No config/sanitize changes. Reviewer delta probe: **zero findings** (data integrity 17/17 nocookie, locked pick, staged-fullscreen panel with the Exit pill still on top at z900000, tiny-widget scroll, chip persistence across station add/remove). **149 tests green.**
- **v6.26 (Sep 19, 2026) — `Deckhand_v6.html` (appVersion 6.26.0) — YouTube gets its own card; Lofi 2.0; the music box is retired.** Croix's field notes: "The scratchy under the lofi is annoying. The lofi is ok, maybe a little underdeveloped. Can you make YouTube a tool of its own…? Youtube is not blocked at school. … the mozart music box sucks." **(1) YouTube widget** (`type:"yt"`, `makeYT`, + Add → Board, sand strip) — separate from Slides so a video card and a deck run side by side. The "direct line" = **STATIONS**: one-tap chips in `wcfg.stations` (≤6 `{name≤20,url}`), seeded with the Lofi Girl stream (`jfKfPfyJRdk`) ONLY when the key is absent (a deliberately emptied `[]` stays empty across reloads — verified); tap = load (PLAY action, live while LOCKED, dirties `wcfg.url` by design like scoring); ☆ Save adds/renames (dedupe by url, cap-6 note); ✕ chips remove (locked-hidden with the paste row). The paste row is **YouTube-ONLY** (embedURL → must be `youtube-nocookie.com`; anything else keep-last-good + "decks and sims go in a Slides widget"), and **sanitize enforces the same rule** — station urls normalize through `embedURL` (hoisted, callable at boot) and non-nocookie results are dropped (adversarial find: a hand-edited station could smuggle any site into the frame). `refocusDeck` now covers `type "yt"` — a staged card owns the clicker keys; `destroy()` blanks the frame so ✕/scene-switch actually silences it. NO `allow=` attr on the iframe (permissions-policy delegation logs console violations over file://). Croix also loves the **Mood Melodies** channel — no station seeded for it (never guess video ids); he pastes any of its links once and ☆ Saves. **(2) LOFI 2.0** (`makeMusic` rewritten, lofi-only): **crackle DELETED** (not attenuated — gone, node and all); the tape got a brain — `LO_TRACKS` (4 sides: key transpose −4..+3, bpm 69–75, progression, motif) each play 32 bars of **8-bar phrases** then a 2-bar chords-only breath and rotate (`whatEl` shows "Lofi — side A/B/C/D"); **motifs RECUR** on phrase bars 2 and 6 (2-bar spans, occasional +12 second pass, ±50ms humanize) — repetition is what reads as music; turnaround bar 7 thins hats + soft 16th snare fill; chords arp-spread 25ms; master chain gained a warm lowpass (5200Hz) and an **echo bus** (delay .29/fb .32/wet .4 → master, pre-duck) fed only by motif notes. `lofiBar(t)` returns its bar length; tick adds what it returns (per-track bpm). Audio verified by analyser: rms .019, peak .25, soak 8.5min crossing 4 side-changes clean, osc census flat. **(3) Mozart synth REMOVED** (channel forced/sanitized to "lofi") — real PD recordings replace it **when Croix attaches his open-source files** (VERIFIED: this workspace's egress proxy 403s music hosts — musopen/wikimedia unreachable; the audio must arrive as chat attachments, then get compressed ~0.5MB/min and base64-baked as a channel). ADVERSARIAL (same reviewer, delta): both v6.25 MEDs verified fixed; 1 LOW (the sanitize/runtime disagreement above) fixed + regression-tested via a `loadFixture` hostile-config boot; held: XSS station names inert, 50-station floods, zombie-audio on close, alarm-overlay chip taps unreachable-but-harmless. **149 tests green.**
- **v6.25 (Sep 19, 2026) — `Deckhand_v6.html` (appVersion 6.25.0) — MUSIC: the self-contained jukebox + YouTube embeds.** Croix: "Maybe some lofi. Or mozart. Or maybe both. Good work/study music." Croix chose BOTH routes. **(1) Music widget** (`type:"music"`, `makeMusic`, + Add → Classroom, teal strip): pure WebAudio, no files/network/licensing. Channels: **LOFI** — endless generative tape (Fmaj7→Em7→Dm7→Cmaj7 e-piano stacks, sine bass, kick/snare/swung-hat noise voices, looping vinyl-crackle buffer, sparse A-minor-penta keys; 72bpm, 120ms tick scheduling 0.35s ahead); **MOZART** — music-box synth (sine + fading 3rd partial) of PD score data: Eine kleine K.525 opening (16 beats) + the full K.265 theme (48 beats — the Twinkle he wrote the variations on) with Alberti eighths, 104bpm, ~66s loop. Graph: voices→`master`(=wcfg.volume)→`duckG`→out; **the crackle feeds `master` too** (routed around it, the pops IGNORED the volume slider — adversarial find). **Ducking**: every tick eases `duckG` to 0.08 while `#alarm.on`, back to 1 after (hooks `api._duck()`, `_level()`). **Pause = SILENCE**: `hush()` fades the master bus in ~40ms (a scheduled lofi bar is ~3.3s of nodes; clearing the tick alone let a drum hit land 2s after Pause — adversarial find); play `unhush()`es; channel-switch hushes the old tail and fades the new channel in at +0.25s; the volume slider writes the bus ONLY while playing (else it would unmute a dying tail). `wcfg={channel,volume}` sanitized; chips/slider/Play live while LOCKED (play actions — yes, a kid at the panel can change channel/volume and light the dirty dot: accepted, same as scoreboard). ctx built on first Play (gesture-safe); destroy closes it. **AUDIO VERIFIED** by analyser probe (scratchpad `audio_probe.js`): both channels audible, peaks < 0.4 (no clipping), levels matched; reviewer's 90s+90s soak = flat oscillator rate, zero leaks, zero errors. **(2) `embedURL` learns YouTube**: watch/youtu.be/shorts/live/embed/playlist (+`m.`/`music.` hosts) → `https://www.youtube-nocookie.com/embed/<id>` (`?list=` kept; bare playlist → `videoseries?list=`); ids validate CENTRALLY (`[\w-]{5,20}`, list ≤64) — a bad id is a clean keep-last-good rejection with a YouTube-specific note, never a truncation and **never the raw unframeable page** (black-widget adversarial find). Slides normalization untouched; staged YouTube gets clicker keys via the v6.22 refocus. ADVERSARIAL: 4 finds (crackle-volume, pause-tail, list-truncation, youtu.be fall-through), all fixed + regression-tested; held: lifecycle races, 3-widget cacophony, spam, duck cycles, hostile configs, round-trip, stage/ghost/settle-routine interplay. NOTE: "Sound On/Off" in the header governs CHIMES only — music has its own Play/Pause (by design, documented here on purpose). **148 tests green.**
- **v6.24 (Sep 19, 2026) — `Deckhand_v6.html` (appVersion 6.24.0) — the settle-in ROUTINE: bell → full-screen count → slides.** Croix: "the timer to take over the whole screen. Then automatically switch to the embedded slides." The settle widget dispatches `deckhand:settle` CustomEvents (`{phase, host}`): **"start"** only on a BELL start (`start(auto)` — only bellWatch passes `true`; a chip's wireButton event arg fails `=== true`, so manual starts NEVER stage); **"end"** (consequence spell expired) and **"reset"** ALWAYS report — the Canvas listener decides by looking at the STAGE, acting only when the settle is `focusedEntry`. Listener: start → `setFocus(settle)` + **reclaim the keyboard** (`window.focus()` + blur the active element — a previously staged deck's iframe was eating Space/Escape) + no-op on `body.landing` (Home keeps the screen; the count still runs); end → `setFocus(first embed)` (per-period decks resolve; setFocus→refocusDeck hands the CLICKER the keys) or `setFocus(null)`; reset → stand the stage down if the settle held it. **The bell always wins**: bellWatch dropped its `!running` guard — a run already in flight is restarted WITH the takeover. Staged type: `.w-settle.wFull` gets hallway-size cqh/cqw clamps (the card clamps read tiny fullscreen — caught in screenshot review; verified no overflow at 1920×1080 + 1280×800 with a 60-char message). ADVERSARIAL (fresh agent — the prior reviewer was cancelled; 4 fixed + 1 accepted): **(F1 HIGH) any manual restart/reset mid-routine stranded the stage fullscreen** (end was gated on HOW the run started) → the always-report/stage-decides design above, + reset stands down; **(F2 MED) takeover from an already-staged deck left keys in the iframe** → the reclaim; **(F3 MED) a kid tapping Start ~20s pre-bell suppressed the routine daily** → bell always wins; **(F4 MED) a ghost timer summoned over the count got buried by the handoff** (the exit pass expires floats) → "riders" (running floated timers) are re-floated/re-stage-boxed over the slides with `ghostHold = 1` (TRUTHY-ancient — 0 reads as "never seen" and re-stamps a full fuse; that exact bug cost one test run); **(F5 LOW, accepted)** on Home the routine counts without staging. TEST NOTES: routine tests shrink `en.cfg.seconds` + `_doneMs()` via `_entry`; a staged embed owns Escape — exit via `#unfocusBtn`; on the FULL default board cascade-stacked widgets can cover each other's ✕ (park boxes via evaluate first). **146 tests green.**
- **v6.23 (Sep 19, 2026) — `Deckhand_v6.html` (appVersion 6.23.0) — the SETTLE-IN widget.** Croix: "students congregating and kinda bumming around at the beginning of class… a countdown of like 30 seconds from when the bell rings. After 30 seconds it's comment cards for anyone still standing up." New widget `type:"settle"` (`makeSettle`, + Add → Classroom, paleturq strip). Three states painted into `.stTop/.stBig/.stSub`: **ARMED** ("AT THE BELL / 30s / to be in your seat", turq), **RUNNING** ("FIND YOUR SEAT" + big seconds, tabular, coral ≤5s — cqw-clamped type, widgets are `container-type:size`), **DONE** (the consequence message big in coral, `wcfg.msgDone` ≤60 default "Still standing? Comment card.", `textContent` only, one `Sound.chime()`, DONE_MS 20s — `api._doneMs()` hook — then re-arms). **The bell starts it hands-free**: a 250ms `bellWatch` on `Bell.statusAt(now())` fires ONLY on a transition INTO a period (prev label ≠ cur, `!running` guard); first tick just baselines, so booting or adding mid-period never fires; verified across HOWL→3rd, Lunch→4th, No-bells (never fires/throws), and a new period starting mid-DONE (fresh run). Start chip + Space restart, R re-arms — **live while LOCKED** (play actions, like +1); ✎ chip toggles `.stRow` (Seconds 5–300 + message; commit on change/blur; Enter/Escape blur; `body.locked` hides `.stEdit`/`.stRow`). Sanitize: whitelist + `seconds` numIn 5–300/30, `msgDone` ≤60. ADVERSARIAL (fresh agent, ~30 probes; interval census, XSS-in-msgDone round trip, 12-cap, twin settles both firing, alarm interplay all held): 2 finds, fixed + regression-tested — **(F1 MED) a week-schedule RE-PICK mid-period fired every settle on the board** (Home → Black Week flips the label instantly; no bell rang) → bellWatch also baselines on `Bell.getActive()` identity change and while `Settings.isOpen()` (label edits at Apply); **(F2 MED) an EMPTIED Seconds field committed 5** ("" → 0 → clamp) → blank/junk now reverts. Accepted notes: ✎ row reopens after unlock if it was open; Escape in the row commits (blur semantics); a settle chip tap mid-stage doesn't refocus the clicker (same as every chip; tap the slides). **145 tests green.**
- **v6.22 (Sep 18, 2026) — `Deckhand_v6.html` (appVersion 6.22.0) — the classroom-failure fixes.** Croix, after a real class: "Dude that was a failure. The boat didn't move. Embed didn't work with my USB clicker so I couldn't use embed." **(1) Frozen boat = the panel's reduced-motion flag** (managed ChromeOS can force it; he'd only ever seen the drift on a machine without it). New `cfg.ui.motion` `"auto"|"on"|"off"` (sanitized; default auto) + Settings → General → **"Waterline motion (boat & sea)"** with a coral hint that appears only when the device is requesting reduced motion ("on Auto, the water holds still") — so the CAUSE is visible in the room. ALL waterline animation rules (`#boat` drift/hop + every sea `.go` rule) moved OUT of the `prefers-reduced-motion` media block and re-scoped under **`body.waveMotion`**, which `applyWaveMotion()` computes from `waveMotionOn()` (on → true; off → false; auto → !matchMedia(reduce)) at boot (Sea.init), on Settings Apply, and on a live media-flag change (listener — the stale-REDUCED snapshot note is fixed too; `Sea.ok()` now calls `waveMotionOn()` live). Widget glides/spawns/alarm pulse still strictly honor the device flag — the override covers ONLY decoration. **(2) Dead clicker = keyboard focus.** A USB clicker speaks arrow/page KEYS; Google Slides hears them only while the IFRAME owns the keyboard, and Deckhand kept focus on itself. New `Canvas.refocusDeck()` (no-op unless the FOCUSED widget is an embed with a visible frame; **skips while a timer's digit entry is live** — those keys are ours): called on stage entry (`setFocus`), after `summonGhost` (the ghost needs no keys), on dock fold (`setDockMin(true)`), and from `Alarm.dismiss` (the ring takes focus by force — dismiss hands it back, so the clicker resumes). The `.embKeys` "keys are in the slides" nag is display:none in stage — there, keys-in-deck is the DESIRED state. CONSEQUENCE, deliberate: **Escape dies while the deck owns the keys** — Exit is the ⛶ pill (touch room; the alarm still force-reclaims, so a ringing timer is always dismissable). TEST NOTES: any test that stages an EMBED must exit via `#unfocusBtn`, never `keyboard.press('Escape')` (the pin test was converted); the suite's reduce pages exercise the override end-to-end through the Settings UI (`#setBtn` → `#sMotion` → `#applyBtn`). Unreproduced from his report: "a few other issues I can't remember" — ask for specifics next field day. **143 tests green.**
- **v6.21 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.21.0) — the fish learns the house style.** Croix: "The fish animation doesn't do it for me. I like the subtle serpent. No big shows, but a little doodle show for the kid who's absolutely bored out of his mind… so he watches the ship." THE AESTHETIC RULE, now explicit for every future sea thing: **the serpent is the register** — slow, low, wordless, inside the wave band; nothing arcs over the dunes; the bored kid watching the ship gets rewarded, the working class never gets summoned. The fish's big parabola (v6.20) violated it. **Now a SKIPPER**: four low hops along the waterline, each smaller than the last (damped, same family as the serpent's damped-sine humps — the math whisper survives), on the shared path `'M8,58 Q38,14 68,58 Q98,26 128,58 Q158,36 188,58 Q218,44 248,58'` (`.pfArc` viewBox AND `.pfFish` offset-path — keep these two IDENTICAL); container 260×64 at left:44vw, bottom:2px — deliberately overlapping the boat's drift range so the fish skips BEHIND the hull (DOM order sea < boat does the occlusion). White `.pfSplash` bursts DELETED entirely; dotted trace peak opacity .4→.3; `pfLeap` 3.2s linear (constant speed reads as skipping); tail flap kept; DUR fish 4600→4200. Reviewer delta-check: all pass — no ink above the dune line at any leap instant, hull ink unbroken at the overlap, rest/gap states pixel-identical to idle, zero splash leftovers, reduce shows nothing, no JS errors. **141 tests green** (no test changes needed — DUR 4200 < the 4800 expiry wait).
- **v6.20 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.20.0) — sea life 2.0: bigger, alive, plus a WHALE.** Croix: "They are a little underwhelming." Diagnosis: pea-sized and cardboard — raw pixels weren't the fix, BEHAVIOR was. Everything grew ~50% and gained secondary motion (every infinite loop gated under `.go`, all inside the no-preference block): **Serpent** (92→136px): humps are now 4 separate `.ssHump` paths with STAGGERED infinite `ssBob` (translateY, .28s delays — the body undulates), and an `.ssSwim` wrapper runs `ssDive` over the same 30s as the glide — it SUBMERGES mid-crossing (translateY 56px, behind `#wavesFront`) and resurfaces: one glide reads as two sightings. **Fish**: 320×120 container, arc apex well above the dune line (`offset-path 'M16,104 Q160,-40 304,104'`), 30px fish with an infinite `.pfTail` flap (`transform-box:view-box; transform-origin:8px 8px` — SVG-child rotation needs both), and two white `.pfSplash` bursts (radiating strokes, scale+fade) at takeoff (.45s) and landing (2.25s); fish DUR 4200→4600. **Buoy** (30→38px): extra `.byBob` wrapper (infinite translateY bob) around `.byRock` — nested wrappers because bob and rock both animate transform — plus white `.byWake` ripple strokes. **NEW: `#sea-whale`** (96px, left:60vw, bottom:2px): `.whSurf` parked at base `translateY(64px)` = fully below the viewport when idle (geometrically verified at 781×541 too), `.go` runs `whSurf` 12s (rise to −14px — 4px was BEHIND the front wave, only a back-sliver showed, caught in screenshot review — hold, dive at 80–100% with rotate(10deg) nose-down) and `whSpout` 12s (two turq scaleY/opacity bursts from origin 30,26). `THINGS`/`DUR` gained whale:12500. ADVERSARIAL (2nd delta re-probe, same reviewer): **zero findings** — whale never paints outside its performance (no end-gap flash at 12.0–12.5s: animation end reverts to submerged base), fish DUR gap is pixel-idle-identical, stage entry mid-DIVE still clears cleanly (v6.18 fix holds), tap-through clean over the surfaced whale, idle animation census = boatDrift only, serialization one-copy, dirty false, no JS errors. **141 tests green** (whale rig asserts folded into the motion test; things list = 4).
- **v6.19 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.19.0) — the sea shows up, and the boat answers.** Croix: "I've been watching for a few minutes and the boat moved back and forth in the center but no surprises or sea monsters." Diagnosis: not a bug — he saw boatDrift (so animations are on); v6.18's FIRST sighting was scheduled 4–9 min out and he watched less than that. **(1) Timing:** first visitor **60–120s** after boot (`FIRST`/`FSPAN`), then every **3–7 min** (`MIN` 180000 / `SPAN` 240000); `tick` now also guards `!busy()` (any `.seaThing.go`) so two visitors never overlap (the 46s buoy could previously still be mid-drift at the next tick). **(2) TAP THE BOAT** — the show-the-class button that doesn't look like a button: `#boat` flipped `pointer-events:none → auto` (the ONLY decoration taking pointer events; no cursor change — it stays a secret) and Sea wires `click → summon()`: refuses while `busy()` or `Alarm.isOn()`, else `pickOne()` (the factored never-same-twice pool). Works while LOCKED (deliberate — decoration, zero config writes); stage/phone hide the boat so it can't fire there. Landing taps work too (visitor swims the landing waterline). ADVERSARIAL (delta re-probe by the v6.18 reviewer): **zero findings** — full-drift-envelope `elementFromPoint` never returns the boat over anything interactive (at 781px the dock z15 wins where they overlap), 20-tap spam = one `.go`, drag-release over the boat fires nothing, digit entry undisturbed, alarm tap at boat coords hits the overlay (dismiss wins), dirty stays false, no JS errors. KNOWN: at ~781px viewport width the boat hides almost entirely behind the dock (sliver reachable) — irrelevant on the panel. Suite: boat-tap single-visitor asserts folded into the v6.18 tests; scheduler no-stack assert in the motion test. **141 tests green.**
- **v6.18 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.18.0) — the boat rides the WATER, and the sea keeps small secrets.** Croix: "it looks like it's riding the sand not the waves… is it possible to do more with it? Keep it small and subtle but add in things like a sea monster or maybe math things. I really like it though." **(1) Waterline fix:** `#boat` dropped bottom:44px → **8px** (44px parked the hull on the sand-colored back dune), and a new fixed **`#wavesFront`** (the front `#8FE5DC` wave path duplicated, bottom:0 h:88px, z0, pointer-events:none, AFTER the boat in DOM) repaints the front wave OVER the hull — the boat visibly sits IN the sea. Same-color-over-same-color, so any subpixel misalignment is invisible. **(2) `Sea` module** — three hidden fixed decorations in `<div id="sea">` (DOM order sea < boat < wavesFront; all z0, aria-hidden, pointer-events:none), each a wordless MATH joke: **`#sea-serpent`** (humps = a damped sine wave + raised neck/eye, glides right→left 30s), **`#sea-fish`** (leaps a fading DOTTED PARABOLA via CSS `offset-path` + `offset-rotate:auto`, ~4s), **`#sea-buoy`** (navy channel buoy marked **π** in paleturq — a turquoise one vanished into the sea — drifting left→right 46s with an infinite-alternate rock on an inner `.byRock`; outer/inner split because drift and rock both animate transform). A thing plays when `.go` lands (`.seaThing{visibility:hidden}`, `.go` shows; animations in the no-preference block, keyframes ssGlide/pfTrace/pfLeap/byDrift/byRock). **Scheduler:** one random thing (never the same twice running) every **4–9 min**, only when `ok()`: not REDUCED, not `document.hidden`, **not focusMode**, viewport >780×540, **not `Alarm.isOn()`**. Landing page plays too — deliberate (waves + boat live there; kids see it at class start). Stage/phone CSS hides `#sea` + `#wavesFront` alongside waves/boat. **Hooks:** `Deckhand.sea('serpent'|'fish'|'buoy')` plays one now (refuses while live; auto-clears via a per-thing DUR map), `Deckhand.seaEvery(ms)` hurries the scheduler. ADVERSARIAL (fresh pass; everything else held — tap-through, dirty-purity across lock round-trips, one-copy serialization round-trip, reduced = zero visible motion, `byRock` compositor-idle when not playing, alarm dismissable mid-swim): **(F1 LOW, fixed + regression-tested) display:none CANCELS a css animation** — a `.go` surviving stage entry (or a ≤780px resize) restarted its glide on return, then the wall-clock removal timer fired mid-screen and the creature popped out of existence → `Sea.clear()` (removes every `.go` + pending timers) now runs on stage entry (`setFocus`, try/catch) and on resize below 781×541. F2 (landing) = intended, above. KNOWN: `REDUCED` is sampled at boot (a mid-session OS toggle needs a reload — fine on a classroom box); the buoy's first ~11s pass behind the dock (z15) before emerging. **141 tests green.**
- **v6.17 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.17.0) — the ⏱ preset menu: the timer is BORN a ghost.** Croix, correcting v6.16's read of his feedback: "I didn't want longer time with the window, I wanted the window to go away completely. So maybe the timer pill up top just asks me to select one of the preset and it kicks out just the timer not the box at all." **(1) `#focusTimerBtn` now opens `#focusTimerMenu`** (in `#focusTimerWrap`, absolute under the pill, navy card, one column, nowrap pills; rebuilt on every open by `buildTimerMenu()`): one pill per `cfg.timer.presetsMinutes` ("N min", `data-min`), **"Until bell"** (`data-bell`; shown only in-period with >30s, RE-CHECKED live at pick ≥5s — same contract as the widget's chip; a pick after the period ends is a silent no-op, accepted + documented), and **"Type a time…"** (`data-custom`) — the ONE road that still shows the card. **(2) `summonGhost(secs)`**: `placeSummoned()` (the old summon body: find-or-create, stage box 66/50/31/44, `float`, select, raise) → new timer api **`startSeconds(secs)`** (clamps ≥1s, kills digit entry, syncs preset-chip aria by `dataset.min`, `startPause()`) → if not itself the focused widget, `ghostHold` backdated past GHOST_FUSE + `.wGhost` applied SYNCHRONOUSLY — wheel + digits from the first painted frame, the card never appears; a tap still lifts it (pause/reset live there). **(3) Escape peels ONE layer:** `Canvas.closeMenus()` returns whether any menu (add/scene/⏱) was open; the key handler calls `unfocus()` only on false. `setFocus` hides the ⏱ menu on every change; the document-pointerdown closer covers `#focusTimerWrap`. ADVERSARIAL (4th staged review, 5 finds, 4 fixed + regression-tested): **(F1 HIGH) clearing a CALMED alarm badge killed a timer RESTARTED since the ring** — `Alarm.dismiss()` reset every api in `ringing`; now it skips any that is `running()` (ring → ignore → summon next timer → clear badge is the NORMAL lesson sequence with born-ghosts); **(F2 MED) locked/cap with no timer in scene = menu of silent no-op pills** — now all pills disable + a `.menuLabel` note ("Unlock to add a timer" / "Board is full"); **(F3 MED) "Type a time…" over a running ghost was dead** — digits gate on `canEnter()` (false while running) and the ghost only lifted on the next 700ms tick; `summonTimer()` now lifts `.wGhost` synchronously, stamps ghostHold, and PAUSES a live run so the card is typable immediately; **(F5 LOW) drag commits wrote 14-decimal floats into config** (the stage-box adoption's `r2()` was immediately overwritten by the move branch; ordinary drags always did this) — `r2` hoisted, EVERY cfg pct write in `up()` (move + resize branches) rounds 2dp. F4 (bell-pill inert in the period's last seconds) = accepted policy, see (1). TEST NOTES: old `#focusTimerBtn` clicks became the `summonCard()` helper (⏱ → `[data-custom]`); after clicking a menu pill, `blur()` before typing digits — Enter on a focused button is swallowed by the `isButton` guard. **139 tests green.**
- **v6.16 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.16.0) — staged adds + the calmer ghost, round 3 of Croix's field feedback.** Field report (timer + slides + sketch pad): ghost editing was "a little weird with the box coming and going, made sizing it kinda weird", and pulling out a sketch pad "crashed out of full screen." **(1) + Add while STAGED now FLOATS the new widget over the deck instead of collapsing focus** — the v6.9-era `setFocus(null)` in addWidget is RETIRED; `staged = !!focusedEntry` and the new widget gets `en.float = true` (session-only, config untouched). Staged spawn spots: the **sketch pad gets a tall LEFT half-panel** ({x:2,y:6,w:44,h:86} — "demonstrate something to the kids next to the slides"), everything else lands along the right edge ({x:max(2,96−ww),y:10}); the ⏱ summon keeps its bottom-right corner, so they never collide. **(2) GHOST_FUSE raised 2s → 8s** (`var GHOST_FUSE`; `Deckhand.ghostFuseMs()` hook unchanged) — the fade must never fight a teacher actively placing or sizing the timer ("the box coming and going"); a ghost now only forms once the timer is truly left alone. ADVERSARIAL FINDS (3rd staged review), all fixed + regression-tested: **(F1 HIGH) staged adds piled on ONE exact-coincident spot and the pile BAKED INTO CONFIG** — a coincidence-walk loop (y+8/x−4, 20-guard) now guarantees distinct spots; **(F2 MED) `en.float` NEVER EXPIRED** — a staged-added widget permanently occluded every later lesson and made ⇈ lie; floats now expire in the `setFocus` exit loop, AND each ex-float gets `bringToFront(e2)` there — z-index is a SNAPSHOT, so without an explicit restack the stale 500000 stratum rode over the next stage anyway; **(F3 MED) the keyboard flow (+ Add → type digits → Enter, never pointer-touched) ghosted ~0.7s after Enter** — a null `ghostHold` counted as "idle forever"; ghostSync now stamps first-seen (`if (!en.ghostHold) en.ghostHold = Date.now()`), guards on `api.entryActive()` (defensive — entry can't start while running), and `summonTimer` stamps on summon. Cleanups: dead `keepFocus` opt dropped (addWidget no longer reads it); stale "2s" comments now say GHOST_FUSE. TEST NOTES: the keyboard-flow test must `blur()` the add-menu button before typing (Enter on a focused button is swallowed by the `isButton` guard, not committed to the entry). **134 tests green.**
- **v6.15 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.15.0) — stage-mode refinements, round 2 of Croix's field feedback.** **(1) Collapsible dock:** `#dockTog` chevron (first dock child, 44px hit box — it's the SOLE control when folded) toggles `body.dockMin` (all children but the chevron hidden, opacity .5); **STAGE AUTO-FOLDS it** — the dock sat exactly on the Google Slides player's ‹ › arrows (Croix: "it was blocking the arrow keys on the slides"); folding closes both dock menus; exit auto-expands. The v6.14 dock "Timer" pill is REMOVED (Croix: redundant) — summon lives in the focus bar; Timer stays in + Add. **(2) GHOST timer** (Croix: "remove the box and everything but the visual wheel and countdown once started. So it's subtle"): `ghostSync` (one 700ms interval in Canvas) gives `.wGhost` to any float/stageBox/pinned TIMER that is RUNNING (`api.running()`, new) over a focused deck — CSS melts strip/chips/buttons/handles/card/`.sel` outline away, leaving the wheel + halo'd digits; any pointerdown on it stamps `en.ghostHold` and lifts the ghost (controls back), 2s idle re-ghosts; pause = card stays; unfocus strips all ghosts instantly. ADVERSARIAL FINDS, fixed + regression-tested: **ghostSync must skip while `dragging`** and `teardown()` stamps ghostHold — a >2s placement drag dissolved the strip UNDER THE TEACHER'S FINGER and re-ghosted on release; and **`setFocus` early-returns on a no-op unfocus** (`!en && !focusedEntry`) — a stray Escape was silently unfolding a deliberately folded dock. **(3) Long-press fix** (Croix: "hold to unlock… my devices kept triggering the little copy paste menu"): global `contextmenu` preventDefault EXCEPT `input/textarea/select` (long-press-paste of Slides links keeps working) + `user-select:none` on button/.pill/.chip/.strip/#dock. Known trade: the Note widget's text lost its touch copy route (Ctrl+C still works). TEST NOTES: dock controls are display:none while staged — tests tap `#dockTog` first, like a teacher would. **131 tests green.**
- **v6.14 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.14.0) — STAGE MODE + the ⏱ summon, from Croix's live screenshot.** Field feedback (his real "Cube Values" deck focused): "Can the embed take the top of the screen too?… the add button is kinda clunky." **(1) STAGE MODE:** while any widget is ⛶-focused, `body.focusMode` (guarded `:not(.landing)` — Home keeps its header) hides the header, waves, boat AND the focused widget's own strip (its buttons sat exactly under the focus bar; the deck deserves every pixel); canvas margins shrink to 10/12/8; the focus bar rides at top:16; `alignGrid()` re-anchors on every toggle. The dock stays (scene switch + summon + Lock still reachable). Tradeoffs, deliberate: the embed's ✎ needs Exit → edit → refocus (3 taps); Settings unreachable mid-stage (Exit is one tap — and consequently a live stage box can never serialize). **(2) ⏱ TIMER SUMMON** (`summonTimer` — `#focusTimerBtn` beside Exit + a `Timer` pill in the dock): one tap = a timer ON TOP, over a focused deck too. Existing timer → `en.float = true`, the SESSION-only twin of pin (bringToFront base is `pin || float`) — config untouched, never dirties, **works while LOCKED**; none → created via `addWidget(type, opts)` (new: `keepFocus` skips the v6.9 collapse — the widget is VISIBLE over the deck via float; `spot` override; returns the entry). During focus, a non-pinned summoned timer gets a compact **STAGE BOX** (inline 66/50/31/44 — a board-sized timer would blanket the slides); Exit restores `applyBox` and clears flags; a REAL DRAG adopts the stage box into config (rounded 2dp) — adoption lives in the RELEASE path guarded by `pend`, because the first version adopted on ANY pointerdown: a mere strip/⇈/⛶ tap permanently overwrote the saved layout (adversarial MED-HIGH — the summon's "never dirties" contract held only if you never touched the timer). Also from review: `#cfgWarn{pointer-events:none}` — the corrupted-copy banner covered the staged focus bar and stranded a touch-only teacher with no header, no strip, no Escape key. TEST NOTES: the focused widget's strip is now display:none in stage — collapse via `#unfocusBtn`, not the strip's ⛶; locking dirties config by itself (ui.locked persists), so dirty asserts must compare across a lock/unlock ROUND TRIP. Adversarial: 2 findings, both fixed + regression-tested; held: 30 summon/exit cycles clean, 100-tap chaos invariants, locked summon writes nothing, stage box never serializes, alarm/calm reachable staged. **130 tests green.**
- **v6.13 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.13.0) — "The feel": the design pass, closing the triple review.** **(1) Press feedback everywhere:** one global `:active{translateY(1px) scale(.97)}` (+ #launchBtn shadow-collapse) — an IR panel never fires :hover, and before this not one control visibly responded to a finger. EXEMPTION: `#lockBtn:active{transform:none}` — a press-shrink slid the edge out from under a stationary 1.5s hold finger and cancelled the unlock (adversarial find). **(2) 44px touch targets:** strip icons ✎⛶⇈✕ inflated via `min-44px + margin:-13px -8px` (were 27×20 INSIDE a drag surface); scoreboard/tally +1 grown to ≥40px with a 16px gap from − (a coarse tap aimed at +1 could subtract); `.scName` given real height/padding **+ `position:relative; z-index:1`** — a mid-pop scaled numeral paints AND input-hit-tests over static siblings (this exact mechanism ate rename taps in the suite AND in the adversarial probe; browser input hit-testing can disagree with elementFromPoint near transformed layers — measured live). **(3) Type fed:** timer digits to 120px (56px sat beside a 175px clock); shed order fixed — chips hide at ≤340 BEFORE digits shrink; disc digits stay small (the hole is fixed — reverting a blind bump the suite caught). **(4) Urgent = readable:** low-time digits and TIME'S UP are navy-on-coral (coral text measured 2.47:1 exactly when the class looks up); the chip centers via `inset:auto + translate(-50%,-50%)`; **`.visWrap.low .bellSmall` must be navy — coral-on-coral made "until bell" INVISIBLE for the last 5 min of every period** (the review's HIGH). **(5) One drawn SVG icon set** (`<symbol>/<use>`, currentColor, one stroke weight) replaces the Unicode glyphs (three sizes, emoji-font pencil); `use href="#…"` verified over file:// WITH `#t=` hashes and in downloaded copies. **(6) + Add menu grouped** TIME/STUDENTS/CLASSROOM/BOARD (`.menuLabel` rows); menus get `max-height + overflow-y:auto` (phone-landscape clipped the whole Time section off-screen, fixed-position = unscrollable). **(7) Phone:** header sheds keyboard hints (`.kbd` spans) + Fullscreen; menus pin `position:fixed` inside the viewport (they overflowed to 518px in a 390px viewport). **(8) Motion, one easing** (`cubic-bezier(.32,.72,0,1)` ~220ms, ALL inside `prefers-reduced-motion: no-preference`): focus/unfocus glide, drag snap-SETTLE (freeze at the finger px, then rAF+glide to the gridline), scene cross-fade, spawn arrival, alarm entrance. `startPointer` strips `.gliding` — a grab mid-settle must drag 1:1 (adversarial: 90px rubber-band). **(9) Grid re-anchored to the CANVAS origin**: `snapTo` drops the page-base; `alignGrid()` shifts the painted background (launch/scene-load/resize) so snapped == drawn — x:0 widgets finally sit on the lines. v6.12 boards load 24px off-line (cosmetic); first drag re-snaps. **(10) Delights:** the sailboat on the waves (45s drift, hops when an alarm is dismissed, hidden on phone/reduced), the +1 pop (overshoot spring), agenda checks that DRAW (svg dasharray) with a wiping strike (`.agText::after`). **Coral returned to the alarm family:** locked button is navy+turquoise. **NOTE: the intentional NUL byte is GONE** — the strip-signature separator is now the `\u0000` ESCAPE (survives serialization; `grep -a` no longer needed; the "binary file" quirk is history). **TEST HARNESS:** the suite runs under `reducedMotion:'reduce'` (context + per-page emulateMedia; several OLD tests reset emulation with `{reducedMotion:null}` — those now restore 'reduce'; a `null` reset silently reverts to no-preference and lets animations swallow input, which cascaded into three phantom failures). Adversarial review: 7 findings (bell coral-on-coral HIGH; menu clip; glide-during-resize; tally pop vs rename; two hold-to-unlock edge cases; NUL note), all fixed + regression-tested. **128 tests green.**
- **v6.12 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.12.0) — "The teaching day": the triple review's classroom wave.** **(1) Alarm de-escalation:** 45s un-dismissed and the full-screen TIME'S UP folds into a corner badge (`#alarm.calm`; navy-on-coral pill, bottom-right) — the fire-drill class walks back in to the CLOCK; while calm, `Alarm.isOn()` is false so the Keys ladder releases (badge is tap-only); a new ring re-escalates + restarts the 45s; test hook `Deckhand.alarmCalmMs(ms)`. **(2) Spawn into empty space:** addWidget scans for a clear rectangle (else cascades by TOTAL count, clamped by the widget's OWN size — an embed's blind 60/50 clamp went off-canvas, adversarial find — and walks back up the diagonal so clamped adds never coincide). **(3) Timer "Until bell" chip** (`.chipBell`): sets AND starts the period remainder; disabled outside class (5s sync; click re-checks live). **(4) Absent today** (`Absent` module + `attachAbsent` panel on picker/groups): session-ONLY by deliberate design (a today-fact; zero config writes — verified in round trip); pick pools and groups filter to present kids, mid-round marks leave the pool, "5 of 5 left · 2 out"; panels re-sync via the 5s tick. **(5) Per-period agenda** (embed's pattern: `perPeriod`/`period`/`pages{}` + `draftFor` written-for commits): `.agWho` caption shows the page; textarea blur-commit is DEFERRED past the row's own controls (Done/pencil stay synchronous — the deferral broke their contract until restructured); emptied pages DELETE (matches sanitize); viewing never auto-creates pages (they'd crowd the 12-key reload cap); `__proto__` blanked as override and page key (it CRASHED commitDraft via Object.prototype.slice — adversarial find; embed got the same hasOwnProperty guards on decks). **(6) Groups by count:** "3 groups / 4 groups" chips (`wcfg.by`/`count`) alongside Pairs/3s/4s. **(7) Save ritual:** download filename is STABLE (`Deckhand_v6.html` — the dated name forced a rename ritual predicted to decay by October; round-trip test updated); dirty-dot Settings open highlights `#dlBtn` (.dlNudge); `#saveNudge` pill on the last-bell in→next transition while dirty (bottom:84px — it covered Lock at 1024w), hides on tap/download. **(8) Focus-exit pill** `#unfocusBtn` (fixed, z 900000 in-canvas): the panel has no Escape and a pinned timer could park exactly over the focused ⛶. **(9) Warning chime** (`cfg.bell.warnChime`, Settings checkbox): `Sound.chime()` once per warn window, keyed date+label+block-START (repeated labels chime each block). **(10) Embed:** rejected links now SAY why (`.embNote`, 8s) instead of silently reverting; select retargets `draftFor` only when no live draft (v6.7 written-for semantics preserved — first fix attempt broke its regression test). TEST TRAP: groups chips are now 5 — position-based `:last-child` selectors hit "4 groups", not "4s". Adversarial review: 7 findings, all fixed + regression-tested. **124 tests green.**
- **v6.11 (Sep 12, 2026) — `Deckhand_v6.html` (appVersion 6.11.0) — "Survive the students" hardening, from the triple review.** A three-perspective hands-on review (adversarial engineer / veteran teacher / Apple design engineer — full text in `Deckhand_Triple_Review.md`; roadmap: 6.11 hardening → 6.12 classroom → 6.13 design) drove this release. **(1) Drag deadlock (the HIGH):** removing a pointer-captured element mid-drag (scene switch, ✕, anything calling clearScene/removeEntry) left the module `dragging` flag latched TRUE forever — all dragging silently dead until reload. Now a `dragCancel` hook is registered per drag; `clearScene()`, `removeEntry(en)` (same-entry only) and `setLocked(true)` cancel it, `lostpointercapture` is a belt-and-braces listener, and a `finished` guard stops double-teardown. **(2) Lock mid-drag** discards the in-flight geometry (cancel on lock + a locked re-check in the release path) instead of committing into a locked config. **(3) Hold-to-unlock:** locking is one tap; UNLOCKING takes a deliberate 1.5s hold (turquoise fill sweeps the pill via `#lockBtn.holding` animation; early release shows "Hold to unlock…"; a completed hold swallows its trailing click via a `held` flag — without that the click instantly re-locked). Keyboard activation (`event.detail === 0`) still unlocks instantly — kids at the panel have no keyboard. TESTS: every unlock in the suite now goes through an `unlock()` hold helper — a quick-click unlock silently leaves the board locked and CASCADES (the "work mode" timeout and a vacuous agenda assert were both this, not real failures). **(4) Scene SWITCHING works while locked** (view action, like ⛶); the ⋯ manager stays locked out. **(5) Corrupted config block** (bad hand-edit): coral `#cfgWarn` banner ("running on defaults — your file on disk is unchanged"), and Download REFUSES while in fallback so garbage can't bake into descendant copies (`cfgFallback` flag). **(6) sanitize bounds:** bell blocks must match `^\d{1,2}:\d{2}$`, ≤20 per list (50k-block/1MB-string configs used to pass); widget geometry invariant x+w ≤ 100, y+h ≤ 100 (off-canvas ✕ was unreachable and Tab-focus scrolled the board sideways — plus `html{overflow-x:clip}`); widget labels dedupe per scene inside the 20-char cap (two hand-edited "Timer"s made the alarm banner ambiguous); scene-name dedupe suffixes now stay INSIDE the 24-char cap (both sanitize and `uniqueSceneName` — 28-char names only round-tripped by coincidence). **(7) Dirty dot honesty:** `SaveState.snap()` excludes `records` via JSON.stringify replacer — the noise game writes a record every quiet second and kept the dot lit all day (a dot that's always on is a dot nobody reads); records still ride along in downloads. Deliberate decision: timer Start/Reset stay LIVE while locked (play actions, like scoreboard +1 and agenda check-off; lock freezes the LAYOUT). **115 tests green.**
- **v6.10 (Sep 11, 2026) — `Deckhand_v6.html` (appVersion 6.10.0) — wave 4: event countdown, tally, agenda + landing-CSS bug.** Croix picked three from the shelf: **Countdown** (`type:"event"`, paleturq strip): title + date (+ optional time) via a ✎ row; "7 DAYS TO GO / Fri, Sep 18", same-day timed events show a live h:mm:ss, "Today!", then "Done — was …"; dates parsed LOCALLY by regex (never `new Date("YYYY-MM-DD")` — that's UTC midnight, a day early for Florida) with `setFullYear` for <100 years; DST-proof day math (verified across Nov 1 2026). **Tally** (`type:"tally"`, turq strip): 1–2 big counters (`wcfg.items`), +1/− clamp 0–9999, scoreboard-pattern in-place rename, **Space = +1 on the first counter**, R resets. **Agenda** (`type:"agenda"`, teal strip): `wcfg.items[{text,done}]` ≤12×80; **tap a row to strike it through — works while LOCKED** (live teaching, like scoring); ✎ textarea one-item-per-line (locked-hidden), checks survive re-edits via first-text-match; R clears checks; shrink-to-fit (note: `<button>` needs explicit `font-size:inherit` — buttons don't inherit). **Landing bug found while screenshotting** (the specificity-beats-state trap, cousin of flex-beats-hidden): `main#canvas{display:block}` out-ranked `body.landing main{display:none}`, so the z-index:1 board painted OVER the landing page whenever layouts collided — fixed with `body.landing main#canvas{display:none}` + regression test; grep for id-strength rules when adding any state-hide. Adversarial review: 6 findings, ALL fixed + regression-tested — the real ones: **(1) Lock with the event ✎ row open left the event editable on a "read-only" board** (`body.locked .evRow{display:none}` + commit guard; a Lock tap now slams the editor); **(2) agenda `justify-content:center` clipped the TOP of an overflowing list beyond scroll reach** (→ `safe center`); (3) shape-valid impossible dates ("2026-13-40") rolled over silently (sanitize now round-trips the built Date); (4) `target()` two-digit-year mapping + year shown in "was …" when ≠ current; (5) sanitize sliced before filtering (junk could push a real counter out — now filter-first, both tally and agenda); (6) scene-switch with the event row open silently discarded the draft (destroy commits like every other tap-away editor). **110 tests green.**
- **v6.9 (Sep 5, 2026) — `Deckhand_v6.html` (appVersion 6.9.0) — PIN (⇈): tools float over the slides.** Croix: "is there anyway to use tools over the embed slides? so I can stick timers and stuff in and move them around to where they make most sense." Every widget strip now has **⇈ `.wPin`** ("Keep on top"): a pinned widget stacks above EVERYTHING on the canvas — **including a ⛶-focused full-board deck** — and still drags/resizes there (pointer capture beats the iframe). Implementation: `bringToFront` now uses **three strata** — unpinned taps (base 0) < the focused widget (250000) < pinned (500000) — all inside the `main#canvas` z-index:1 stacking context, so dock/settings/alarm still win; un-focusing re-stacks the widget out of the focused stratum (`bringToFront(was)` after clearing `focusedEntry`), and un-pinning drops a widget back UNDER a focused deck instead of tap-raising over it. `syncPin(en)` (aria-pressed + restack) runs on every mount BEFORE the `dataset.wired` guard, so the re-used static clock card carries pin state correctly across scene switches. `wcfg.pin` persists (sanitize: `bool(w.pin,false)` in the common widget mapping; junk coerced). ⇈ **hides while locked** (`body.locked .wPin{display:none}` + a JS guard) — pin edits saved config, unlike view-action ⛶. Adversarial review found 2 real bugs, both fixed + regression-tested: **(1) + Add during focus mode birthed the new widget invisibly BEHIND the deck** and keyboard-selected it (invisible timer ringing minutes later) — `addWidget` now collapses focus first; **(2)** DEFAULT_SCENES/addWidget cfg literals lacked the `pin` key, so pin-try-and-undo on a scene-less board left a phantom dirty dot — `pin:false` added to all widget literals. Text widget's ✎ moved before ⛶ so strips read ✎ ⛶ ⇈ × consistently. **105 tests green.**
- **v6.8 (Sep 5, 2026) — `Deckhand_v6.html` (appVersion 6.8.0) — the noise GAME.** Croix's field verdict on the meter (works on his Windows laptop; Chromebox still policy-blocked): "no actual deterrent or tracking or anything that the kids care about." Rebuilt as a game with visible stakes: **quiet-streak clock** (big tabular m:ss, climbs only while the room stays under the limit), **strikes** (sustained over-limit for >2s — one cough is free — coral ✖ appears, widget flashes, streak dies; >5 shows "✖ × n"), and **per-period records** ("Record 12:05 — held by 4th") — Deckhand knows which class is in the room, so periods COMPETE across the day; records persist in `wcfg.records{period:secs}` via download-copy (sanitize: ≤12 keys, __proto__ guarded). Period rollover (5s check) gives each class a clean slate of strikes/streak; **R** resets the round (records survive). Strikes carry no automated consequence — that's the teacher's call in the room. Tested DETERMINISTICALLY: getUserMedia mocked with a gain-controlled OscillatorNode→MediaStreamDestination (launch arg `--autoplay-policy=no-user-gesture-required`), driving the exact quiet→strike→recover→record sequence. **101 tests green.**
- **v6.7 (Sep 5, 2026) — Focus mode + embed row auto-hide, from Croix's first real embed session.** **THE EMBED TEST PASSED ON REAL HARDWARE**: Croix's photo shows a published deck ("Adding Rational Numbers", clean Google Slides player) inside the SLIDES widget on the Promethean via the Chromebox — filter, framing, file://, all confirmed live. His feedback drove this release: **(1) Focus mode (⛶ in every widget strip)** — one tap expands ANY widget to fill the whole canvas (deck for teaching, timer for tests, clock for silent work); tap again or Esc restores the exact prior box; the config is untouched (never marks unsaved); works while LOCKED (it's a view action — ⛶ has no `body.locked` hide rule); drag/resize disabled while focused; only one widget full at a time; scene switch and widget close un-focus. **(2) Embed link row auto-hides** once a deck is loaded ("couldn't get the link to go away") — ✎ pencil in the strip (`.wEdit`, hidden when locked) brings it back; the row only re-tucks when a NEW deck actually loads, and reopening always shows the CURRENT deck's URL. **Draft-targeting fix found while testing**: the URL input now remembers which period its text was written for (`draftFor`) and commits there — a stale draft can no longer overwrite a different period's deck when the select or the bells move on (this was a real data-corruption path: ⟳ with an old draft open silently replaced 2nd's deck with 3rd's URL). Note `.embRow[hidden]` needed an explicit `display:none` (the flex-beats-hidden trap, third occurrence — grep for it when adding any new `display:` element that toggles `hidden`). **100 tests green.**
- **v6.6 (Sep 3, 2026) — the workflow trio: scene manager, unsaved-changes tracking, per-period decks.** **(1) Scene manager**: ⋯ button beside the scene dropdown → popup (name field + New / Duplicate current / Rename current / Delete current); New spawns clock-only, Duplicate deep-copies widgets (JSON — no aliasing, review-verified), names dedupe with "(2)", 8-scene cap disables New/Dup, **Delete is two-tap ("Really delete?" turns coral) and DISARMS on any scene change or menu close** — an armed confirm can never fire against a different scene (review catch #2). **(2) SaveState module**: baseline `JSON.stringify(cfg)` at boot; coral dot on the Settings pill (+ title) whenever the live board differs from the file on disk (2.5s repaint); `beforeunload` warning while dirty; Download configured copy calls `markSaved()`. Honest by construction: sanitize pre-writes every factory field, so merely *viewing* scenes never dirties (review-verified). `window.Deckhand.dirty` test hook. **(3) Per-period decks** — the embed card joins the bell schedule like the picker did: "Per-period" checkbox + period select ("Auto — 2nd", ✓ marks periods holding decks); `wcfg.decks{label:url}`; Auto follows the bells on a 5s tick (passing pre-loads the ARRIVING class's deck; focused drafts and the period select are never clobbered mid-choice); manual override persists (`wcfg.period`; an orphaned override stays visible instead of masquerading as Auto — review catch #4); per-period keep-last-good; turning the mode on seeds the current period from the single URL; **⟳ reloads `activeURL()` — the deck actually showing, never the stale single-mode url** (review catch #1, the HIGH); sanitize cleans decks (≤12×20-char keys, https-only, `__proto__` blocked, prototype-pollution regression-tested). New global helpers `bellPeriodLabels()` / `currentPeriodLabel()`. Adversarial review: 4 findings, all fixed + regression-tested. **99 tests green.**
- **v6.5 (Sep 3, 2026) — THE HEADLINER: the Slides / web embed card.** Greenlit by Croix's Chromebox probe (framing docs.google.com works from file:// through the district filter). **`type:"embed"`** ("Slides / Embed" in the menu, navy strip, spawns 48×72%): paste ANY Google Slides link — `embedURL()` normalizes edit/view/pub/multi-account `/u/N/` paths (case-insensitive, http too) to the clean `/embed?start=false&loop=false&delayms=60000` player, preserves `slide=` deep-links, and leaves an already-`/embed` link untouched so deliberate kiosk params survive; PhET/GeoGebra/Desmos URLs pass through. https-only everywhere (`javascript:` can't reach `frame.src`, enforced in sanitize AND the input path). **Keep-last-good**: garbage typed over a working URL restores the old one — a live deck is never erased; the alarm's focus steal never commits a half-typed URL (`applyInput` yields to `Alarm.isOn()`). URL row hidden when locked (clean student view); ⟳ reloads; resize handles padded away from the row's controls. **THE CRITICAL CONSTRAINT — keyboard focus**: when the teacher clicks into the deck, the iframe owns the keyboard (that's how you advance slides); a sand pill appears — "Keys are in the slides — tap the board background to give them back" — re-checked on a 300ms poll while the window is blurred so deck-to-deck focus hops always label the RIGHT deck; **`Alarm.ring()` now forcibly reclaims focus (`window.focus()` + focusing the overlay), so a ringing timer is ALWAYS dismissable with Space/Esc/Enter even mid-presentation** (regression-tested live: timer expires while the frame has focus → Space dismisses without a click). Scene switch AND Home both unload the iframe (`deckhand:home`/`deckhand:board` events) — no video audio ghosting under the landing page; returning restores it. `render()` compares the src ATTRIBUTE so Settings-Apply never reloads a playing deck. Adversarial review: **7 findings + 2 design notes, all fixed** (multi-account `/u/0/` links silently blank-framing was the big one — that's every teacher with school+personal accounts signed in). **95 tests green.**
- **v6.4.1 (Sep 2, 2026) — text widget v2 + field findings from the first live day.** Croix ran Deckhand in class (assessment day) and asked for two Note changes: **(1) Edit button hidden** — it's now a small ✎ pencil in the title strip (hidden when locked, like ✕); tapping the text itself also edits (inert when locked — a locked board is read-only); a Done button appears only while editing. **(2) Text organization** — lightweight formatting rendered line-by-line with textContent: `# ` big navy heading, `## ` teal subheading, `- `/`* ` turquoise bullets, `1)`/`1.` numbered lines with hanging indent, blank lines space; structured/multi-line notes left-align (a single plain line stays centered); **shrink-to-fit** steps the font down until the whole note fits (ResizeObserver refits on widget resize) — no scrolling agendas. Mic-blocked message made honest ("blocked by the browser or device policy — Deckhand can't override this") after the Chromebox test. All tests green.
- **v6.4 (Sep 1, 2026) — `Deckhand_v6.html` (appVersion 6.4.0) — Phase 4 wave 3: QR code, noise meter, sketch pad + the z-order fix.** **Z-ORDER BUG (Croix's tablet): `main#canvas` now has `z-index:1`, making it its own stacking context** — widget `zTop` grows with every tap and had climbed past the dock's z 15, burying the + Add menu under an often-touched clock; now widgets can never out-stack dock(15)/settings(20)/alarm(30). **QR widget** (`type:"qr"`, paleturq strip): a **fully in-file QR encoder** (byte mode, EC-L, versions 1–10, GF(256) Reed-Solomon, format+version info, ~250 lines) — no library, verified in the test suite by DECODING its output with an independent reader (jsQR) at every version boundary incl. UTF-8/emoji; paste a link ≤ ~270 chars, students scan from their seats; `wcfg.url` persists. Encoder exposed as `window.Deckhand.qr` for tests. **Noise meter** (`type:"meter"`, sand strip): tap Enable → getUserMedia + AnalyserNode RMS with EMA smoothing; level bar vs a draggable limit (`wcfg.limit`), coral fill + TOO LOUD + coral widget outline when over; degrades to plain messages when the mic is missing/blocked; destroy stops tracks + closes the AudioContext; a `pending` flag stops double-tap from leaking a second, unstoppable stream (review catch). **Sketch pad** (`type:"draw"`, tan strip): 1280×960 canvas CSS-scaled (strokes survive widget resize), 5 palette pens + eraser + Clear, pointer-captured single-finger strokes, R wipes; content deliberately session-only (a bitmap would bloat the config). Add menu now 13 widgets. Adversarial review: 4 findings — 1 rejected as false-positive (claimed Google Fonts was newly added; it's been the documented allowed exception since v1 — reviewer's grep hit binary-detection on the NUL byte in the strip signature), 3 fixed + regression-tested (mic double-tap leak; QR claiming "300 chars" when v10-L caps at 271 bytes — input and message now honest; color chips all announcing "Pen color" to screen readers). **91 tests green** (incl. fake-mic meter test and in-page-encoder → independent-decoder round trip).
- **v6.3 (Sep 1, 2026) — appVersion 6.3.0 — Phase 4 wave 2: text box, work symbols, dice, scoreboard + the "+ Add" menu.** **Text/announcement** (`type:"text"`, grid-tan strip): tap Edit (or the text) → real TEXTAREA (isField-protected typing), blur/Esc/Done commit to `wcfg.text` (≤2000, survives download — verified byte-stable through two generations with `$&`, `</script>`, emoji, backslashes); blur-vs-click race flag-guarded. **Work symbols** (`type:"work"`, navy strip): SILENT/WHISPER/PARTNERS/GROUP WORK color-coded badge + sub-line, chips or Space cycle, `wcfg.mode`. **Dice** (`type:"dice"`, sand strip): 1–3 SVG pip dice, shake-roll (reduced-motion instant), honest total line, `wcfg.count`. **Scoreboard** (`type:"score"`, teal strip): 2–4 teams, +1/− clamped 0–999, tap-name inline rename (in-place button swap — a full repaint would eat the +1 tap mid-rename), R zeroes the round, `wcfg.teams`. **Dock consolidated**: per-type buttons became a "+ Add" popup menu (same button ids inside; outside-tap and Esc close; items disabled at cap/clock-singleton; whole menu disabled+closed when locked). sanitize grew per-type field cleaning (text/mode/count/teams; picker/groups period, groups-only size). Adversarial review: **5 findings fixed + regression-tested** — the theme was inline editors fighting the global input layers: rename/textarea keydown handlers were shielding the ALARM's swallow-all stage (hard-constraint violation — `if (Alarm.isOn()) return` before any stopPropagation), rename's blur-repaint detached the +1 button mid-tap, a slid-off Done tap stranded the race flag, sanitize kept fractional dice counts and whitespace team names, Esc couldn't close the add menu. **87 tests green.**
- **v6.2 (Sep 1, 2026) — appVersion 6.2.0 — Phase 4 wave 1: rosters, name picker, group maker.** Croix set the north star: full ClassroomScreen replacement (his district blocks the site). **Class rosters** live in config (`cfg.rosters[{period,names[]}]`, ≤12×80, edited in a new collapsed Settings section — one textarea per bell-period label, auto-generated, Lunch excluded, orphaned periods kept editable). **The killer feature ClassroomScreen can't do: period awareness** — picker and group maker read `Bell.statusAt(now())` and load the roster for the class that's in the room (passing → the arriving class), with a manual override select per widget (persisted in `wcfg.period`; select blurs on change). **Picker** (`type:"picker"`, paleturq strip): 900ms spin (instant under reduced-motion), **no-repeat pool** — nobody picked twice until the whole round is done ("7 of 22 left"), R restarts the round, Space picks. **Groups** (`type:"groups"`, teal strip): Pairs/3s/4s chips (`wcfg.size`), round-robin shuffle, sizes differ by ≤1, nobody alone, groups never swell past k+1 (7 kids in 4s = 4+3). Adversarial review: **10 findings, all fixed + regression-tested** — the big theme was the roster↔bell-label string join failing SILENTLY (wrong class picked with no cue): no-roster blocks now say so instead of guessing, Apply re-keys the roster grid so renamed periods orphan visibly, label caps are symmetric (20 chars both sides of the join), the 5s Auto-refresh never rebuilds a select the teacher is touching, mid-spin reset cancels the spin, board selects blur when a board key is pressed (Space/R can't die parked on a dropdown), empty-state hints survive the small-widget CSS ladder, gatherRosters mirrors sanitize's caps. **80 tests green.**
- **v6.1 (Sep 1, 2026) — appVersion 6.1.0** — first-day-of-use feedback from Croix (he ran v6 in class Sep 1): **(1) Across-the-room readability** — the bell countdown line, period hero, week pill, day-strip chips, and bathroom pill all got substantially larger cq clamps ("39 min until bell" now up to 40px, chips up to 22px), bell bar thickened; **(2) Next Bell widget** (`type:"bell"`, "+ Bell" in the dock, sand strip): a draining ring synced to the bell schedule — in class it drains through the period ("39 min / until bell · 2nd"), coral inside the warn window, sand during passing ("passing → 3rd"), and out of school it drains the whole gap ("18h 20m / until 1st · Monday 9:20"); honors the "Seconds in countdown" toggle via exported `Bell.fmtSpan`; purely visual (the real bell rings, the widget never alarms; Space/R/digits are no-ops on it); **(3) Buttery drag** — during a drag the widget follows the pointer 1:1 via `transform` only (rAF-throttled, `will-change`, no snapping and no config writes mid-drag); the 48px grid snap and the % config write happen once on release. Same for resize (live unsnapped, snap on drop, so an over-shrink lands on the nearest gridline at/above the 180×140 floor). **(4) Small-widget overflow fixed** (Croix's tablet screenshot: the ring painted over the TIMER strip): `.wBody` clips, and `@container` height steps shed chrome progressively — ≤430px hint hidden; ≤340px smaller ring + ring digits; ≤250px preset chips hidden, slim strip; ≤190px no ring at all — plain digits with just Start/Reset (keyboard entry still works). Removing the leftover v5 id-based button CSS (`#startBtn/#plusBtn/#resetBtn`) was part of this — id specificity was keeping the FIRST timer's Reset button v5-sized at every scale; button colors live inline in `TIMER_BODY_HTML`. **(5) Five resize handles per widget** (Croix: "more drag points"): both bottom corners (brackets) plus left/right/bottom edge nubs, each moving only its own edges (`startPointer(en, e, "resize", dir)` with e/w/s flags; a west-side pull keeps the right edge pinned; only moved coordinates are written to config, so an east-side resize can't float-drift x). No top handles — the strip is the move handle and holds ✕. **(6) The clock widget sheds too** (Croix's tablet, round two): its own `@container` ladder — ≤430px drops day strip + bathroom pill; ≤340px drops bar + week tag, shrinks hero/sub, and the time digits become height-capped (`min(23cqw, Ncqh)` — a wide-short clock must not starve the lines below); ≤250px drops AM/PM + countdown line; ≤190px is time only. 76 tests green.
- **v6.0 (Aug 31, 2026) — Phase 3: the board is a free-form widget canvas with named scenes.** Widgets (clock / countdown timers / stopwatches) drag by their title strip, resize by a corner handle, **snap to the painted 48px background grid in page coordinates** (so they land ON the visible gridlines), stack to front on touch, and store positions as **percentages of the canvas** in config (projector resolution changes can't scramble a layout). **Scenes** are named arrangements in `cfg.scenes` (Croix ships with "Daily Board" and "Stations" — 3 station timers + stopwatch); the dock (bottom-left navy pill bar) holds the scene dropdown, + Timer / + Stopwatch / + Clock, and **Lock** (coral when on; hides ✕/handles, freezes drag/add — students touch the board; timers stay usable). **Multi-timer**: `makeTimer`/`makeStopwatch` factories; the selected widget (turquoise outline) receives Space/R/digit-entry; **S cycles selection**; clicking the clock deliberately keeps the current selection; the alarm overlay names every ringing timer ("Station 2 · Timer") and dismiss resets them all. First timer in a scene gets the legacy ids so v5 CSS/tests carry. Config `schemaVersion` 4 (`ui{locked}`, `activeScene`, `scenes[]`; 3→4 migration defaults them; sanitize caps 8 scenes × 12 widgets, one clock per scene, clamps coords, dedupes names). Old TIMER/STOPWATCH tabs died — a stopwatch is now its own widget. **71 automated tests green** (`test_deckhand_v6.js`); adversarial review found 6 (ghost clock at boot for saved clockless scenes — the one critical; scene-dropdown focus swallowing the keyboard where an arrow key could nuke running timers; uncapped addWidget vs sanitize's 12; narrow-layout drags corrupting stored coords; multi-touch pointerId confusion; duplicate auto-labels after close-and-re-add) — **all fixed + regression-tested**.
- **v5.3.3 (Aug 30, 2026, Sunday night) — `Deckhand_v5.3.3.html`** — Croix caught a real design error live: the out-of-school bar FILLED as time elapsed, so Sunday night showed a mostly-full bar that read as "most of the weekend left" to anyone fluent in the draining ring/tide timers. Arithmetic was right (verified: 81% elapsed), direction was backwards for a countdown. **The "next" bar now DRAINS like the visual timers** — Sunday 9pm shows a ~19% sliver. In-class/passing bars still fill toward the bell (progress semantics). 59 tests green with flipped expectations.
- **v5.3.2 (Aug 31, 2026) — `Deckhand_v5.3.2.html`** — bell countdown is **minutes-only by default** ("39 min until bell", "starts in 2 min", weekends "in 45h 20m", final minute "< 1 min") with a "Seconds in countdown" toggle (`bell.countdownSeconds`, default false) restoring m:ss. Saturday + after-school coverage of the gap bar verified with tests and screenshots (Sat noon ≈ 31% of Fri-4:00→Mon-9:20; Tue 4:30pm ≈ 3% of the overnight span). 59 tests green.
- **v5.3.1 (Aug 30, 2026) — `Deckhand_v5.3.1.html`** — the out-of-school "next" state now carries a **visual countdown**: the progress bar spans the whole gap from the previous last bell to the next first bell (sand-filled, like passing — a weekend is just a very long passing period), so Friday evening shows an almost-empty bar and Sunday afternoon ~72% drained. 55 tests green.
- **v5.3 (Aug 30, 2026) — `Deckhand_v5.3.html`** — Croix's big polish list: **next-class countdown** (weekends/evenings/early mornings show "Next: 1st Period · TEAL WEEK · Monday 9:20 · in 18:20:00" — scans up to 14 days on the nudged timeline, resolves each target day's week via rotation, previews that day's strip; toggle `bell.showNextClass`); **frame-rate-smooth visual timers** (rAF while running, reduced-motion honored); **bathroom window pill** (`bell.bathroom{enabled,firstMinutes,lastMinutes}` default 10/10 — coral "Bathroom closed · opens 10:26" / turquoise "open · closes 10:59", in-class only); **end-of-class warning** (`bell.warnMinutes` default 5 — coral countdown + pulsing bar, forces the countdown visible even when toggled off); **Settings reorganized into 4 collapsible sections** (General / Clock & Bells / Timer / Week Schedules — schedules collapsed by default; Tab trap and open-focus are closed-section-aware); **chime volume slider** (`sound.volume`); **clock seconds hidden by default** (`clock.showSeconds` option); day strip abbreviates long labels ("HOWL Time"→"HOWL", full name stays in the hero); **passing mode is visible** (sand banner + enlarged hero); animated **tide waves**; bigger touch targets. 53 automated tests green; adversarial review (2 findings: closed-`<details>` keyboard trap, nudge-across-midnight scan errors — both fixed + regression-tested).
- NOTE: the cloud workspace was reclaimed between sessions (Aug 2026); v5.0.1 was rebuilt from the conversation blueprint and re-verified — 20 automated tests green, screenshots inspected. **Croix should keep the delivered file AND this handoff in Google Drive**; session workspaces are ephemeral.
- Future: v7 = Phase 4 (widget & embed catalog, Slides embed card first).

## Current feature inventory (v6)

**Canvas & scenes (v6)**: `main#canvas` holds absolutely-positioned widget cards (`container-type:size`; all inner type scales with `cqw/cqh/cqmin` clamps). Drag = pointerdown on the strip with `setPointerCapture`; resize = corner handle (min 180×140); both snap 48px in page coords and are ignored while locked, in the ≤780px stacked fallback (which force-stacks widgets statically), or from a second simultaneous touch (pointerId-filtered, one drag at a time). `removeEntry` splices the widget out of the scene config (the static `#clockWidget` is hidden, never removed — and `clearScene` hides it unconditionally so a saved clockless scene can't boot a ghost clock). `addWidget` cascades positions, refuses a 13th widget (dock buttons disable at the cap), allows one clock per scene, and picks the smallest unused auto-label so alarm names stay unambiguous. Scene switch tears down timers (`destroy()` clears timeouts/rAF); the scene dropdown blurs itself after a change so the keyboard always belongs to the board. **Compact fallback** (≤780px wide OR ≤540px tall — phones either orientation, tiny windows): widgets stack full-width at natural height (`container-type:inline-size`, since size containment needs a fixed height), page scrolls, handles/waves hidden, dock goes static below; drags are disabled there so stored coordinates can't be corrupted. First cut of this fallback chopped widgets to a fixed 320px — Croix caught it on his phone Aug 31 ("butchered my boy"); reworked same day.
**Landing page** (every open; it IS the weekly ritual): time-of-day greeting ("Good morning, Mr. Shaffer" — name in config, editable), date, **THIS WEEK'S BELLS** picker (Teal Week / Black Week / No bells chips), "Open the Daily Board" button. Keys: Enter/Space launch, 1–N pick (hint auto-matches), F/M work. "Home (H)" pill on the board returns.

**Week rotation (v5.1)**: `bell.autoWeek` + `anchorMonday` ("2026-08-31") + `anchorWeekName` ("Black Week"). Weeks alternate between the FIRST TWO groups from the anchor Monday (DST-safe parity; non-Monday anchors snap to their Monday); the landing pre-presses the computed chip. **Manual chip tap still overrides for the day**; groups 3–4 are manual-only picks. The switcher lives in Settings ("Rotate weeks automatically" + "Week of… is…" — anchor compared by slot index, so renames don't count as changes; turning rotation OFF preserves the current pick; explicit No-bells survives even a rotation change). **Weekend rollover guard**: a machine left running adopts the new week at the date change unless a manual override is active (`deckhand:weekchange` event refreshes landing chips).

**Day sequence strip (v5.1, Croix's "which class next" tool)**: under the bell bar, the day's block order as small chips — past faded, current turquoise, next dashed during passing. Kills the Black-week "wait, what's next?" confusion (the day runs 6th→5th→…→1st). Toggle in Settings ("Day sequence strip", `bell.showDayStrip`). Labels rendered via textContent (user config = no HTML).

**Bells**: **Wednesday compression is automatic** (`getDay()===3` + non-empty wednesday blocks; readout shows "(Wed)"). Clock-card readout "Teal Week · 2nd · 39:00 until bell" + thin turquoise progress bar (sand during passing: "… · Passing · 3rd in 2:00"). Pre-school window 90 min. After last bell/no pick: hidden. **Nudge ±600s** shifts bell math only, never the clock. Weekend days simply follow the picked group's regular times (harmless; he can pick No bells).

**Timer widgets** (v6: multiple instances; S selects, no more tabs — stopwatch is its own widget): presets from config (chips). Calculator-style entry on the SELECTED timer: stopped, type digits (7,3,0 → shows raw "0:73" = 73s, normalized on start); Enter/Space/Start commits (becomes new Reset target, deselects chips); Backspace edits; Esc cancels; digits inert while running (hint adapts). +1:00 works while running (visMax ratchet keeps visuals truthful). Last 10s coral. Background timers keep running and ring. Full-screen coral "TIME'S UP" + the ringing timers' names + 3-tone chime (660/660/880); click/Space/Esc/Enter dismisses and resets all ringing timers.

**Settings panel** (proper modal: focus moves in, Tab trapped, Esc closes, focus restored): display name, "Bells at startup" select (may be No bells), nudge, presets (1–6 values, 1–120 min), progress-bar + sound checkboxes (sound applies live AND at startup), **4 week-group editors** (name + regular textarea + optional Wednesday textarea; line format `label start-end`; times 6:00–11:59 AM / 12:00–5:59 PM; empty regular = removed; Wednesday-without-regular rejected; duplicate names rejected; validation atomic — any error applies nothing). Apply keeps the current week pick by name, then slot (renames survive); explicit No-bells picks are never resurrected.

**Download configured copy** (in Settings): the file re-serializes itself — pristine `outerHTML` snapshot at boot, config block regex-swapped, Blob download named `Deckhand_v6_YYYY-MM-DD.html` (real date even in SIM). Post-replace verification aborts rather than download stale config. Byte-stable across generations. This is how layouts persist: arrange, lock, download, replace the daily-driver file.

**Config** (`<script id="deckhand-config" type="application/json">` near top of file, hand-editable): schemaVersion 4 — `ownerName`, `ui{locked}`, `activeScene`, `scenes[{name, widgets[{type: clock|timer|stopwatch|bell, x,y,w,h (% of canvas), label}]}]`, `sound{enabled,volume}`, `clock{showSeconds}`, `timer{presetsMinutes,defaultSeconds,style}`, `bell{…, defaultGroup, groups[{name, regular[], wednesday[]}]}` (max 4 groups). Boot pipeline: parse → **migrate** (chain 1→2→3→4; v1 `bell.enabled:false` → defaultGroup "none"; v2 pairs "X Regular"+"X Wednesday" → "X Week", unpaired keep names, defaults map exact-name-first; 3→4 just bumps — sanitize supplies default scenes/ui; string schemaVersions tolerated) → **sanitize** (every field type-checked with per-field fallback; group/scene names deduped; scene coords clamped; ≤8 scenes × ≤12 widgets, one clock each; corrupt JSON → defaults with bells off). Each module boots in its own try/catch — nothing can take the clock down.

**Test/preview hook**: open with `#t=2026-09-02T10:30` appended → simulates that moment (coral SIM tag; timers still run at real speed). Debug API: `window.Deckhand.config`, `.bellStatusAt("2026-09-02T10:30")`, `.setSchedule(i)`.

## 2026-27 bell schedule (baked into v5.0.1 config; 7th-grade track)
- **Teal regular**: 1st 9:20-10:13 · 2nd 10:16-11:09 · HOWL 11:12-11:43 · 3rd 11:46-12:39 · Lunch 12:42-1:12 · 4th 1:15-2:08 · 5th 2:11-3:04 · 6th 3:07-4:00
- **Teal Wednesday**: 1st 9:20-10:03 · 2nd 10:06-10:49 · HOWL 10:52-11:23 · 3rd 11:26-12:09 · Lunch 12:12-12:42 · 4th 12:45-1:28 · 5th 1:31-2:14 · 6th 2:17-3:00
- **Black regular**: 6th 9:20-10:13 · 5th 10:16-11:09 · HOWL 11:12-11:43 · 3rd 11:46-12:39 · Lunch 12:42-1:12 · 4th 1:15-2:08 · 2nd 2:11-3:04 · 1st 3:07-4:00
- **Black Wednesday**: 6th 9:20-10:03 · 5th 10:06-10:49 · HOWL 10:52-11:23 · 3rd 11:26-12:09 · Lunch 12:12-12:42 · 4th 12:45-1:28 · 2nd 1:31-2:14 · 1st 2:17-3:00
(Structure: outer periods mirror between week types; 3rd/Lunch/4th are the fixed midday pivot. Source: official 2026-27 sheet, Croix's PDF, Aug 30 2026.)

## Engineering practices (keep these)
- Timer accuracy from `endAt`/`startedAt` timestamps re-read on a 200ms repaint — never decremented counters; `visibilitychange` re-syncs countdown, stopwatch, and bell readout.
- ONE AudioContext, created/resumed on first gesture (capture listeners), reused; last-chance resume at ring.
- Pointer events + blur-after-pointer-click on all buttons (Promethean touch works; Space-after-click can't re-fire the button; keyboard activation keeps focus for a11y).
- Alarm overlay (z-30, above settings z-20) swallows ALL plain keydowns incl. auto-repeats; `e.repeat` guarded below it.
- Keyboard priority: alarm → repeat guard → settings Tab trap → form fields → settings → landing → selected-widget entry → entry-start → globals (Space/R act on the selection; S cycles selection; F/M/H). Dock buttons and the scene select blur after pointer use so shortcuts never die parked on a control.
- QA pipeline per release: Playwright suite against `file://` (currently **71 tests**: bell math against real times, canvas drag/resize/snap/z-order, scenes, lock, multi-timer routing, migrations via inline fixtures, settings atomicity, focus trap, hostile configs, download round-trip) + screenshot review + fresh-context adversarial review agent, findings fixed with regression tests. Test file: `test_deckhand_v6.js`.

## Roadmap
- ~~Phase 1~~ DONE (v2, accepted). ~~Phase 2~~ DONE (v3–v5: config, bells, landing, download-copy). ~~Phase 3~~ **DONE (v6: canvas + scenes + multi-timer + lock)** — awaiting Croix's touch-panel verdict.
- Phase 3 as planned (kept for reference): free-form widget canvas + scenes. Croix explicitly wants free-form (likes ClassroomScreen's canvas). Feasible at his scale (~300–500 lines): pointer-event drag with capture, corner-handle resize, click-to-front z-order, positions in config as percentages (survives resolution changes), **snap to the 48px background grid** (on-brand). **Named scenes** = saved widget arrangements ("Warm-up", "Stations", "Test day"), stored in config via download-copy — ClassroomScreen Pro's saved-screens feature, free. Skip: multi-select, undo/redo (reset-to-scene covers it). **Multi-timer falls out as multiple timer-widget instances.** To solve: alarm behavior with multiple timers (per-widget ring state; takeover names which timer), shortcut ambiguity (selected-widget concept), **layout-lock toggle** (students touch the board), **CRITICAL: iframe focus stealing** — when a Slides embed card has focus, all keyboard events go to the iframe; must detect focus loss (window blur), show "shortcuts paused" indicator, reclaim on outside click.
- **Phase 4: widget & embed catalog** — Google Slides embed card (see below), random student picker, group maker, review-game scoreboard, work symbols (voice-level card), visual timer (shrinking disc), drag-drop PDF/image/video cards (object URLs; re-dragged each session — no storage). **QR + sound meter: LOW priority per Croix.** Scenes should unload hidden iframes (memory on a school machine).
- **Optional "Phase 2.5" (liked in principle): Google-as-data-layer** — fetch config/schedule overlay from a Sheet published as CSV at load; offline-first (baked config is source of truth; fetch failure falls back silently). Also the "Sheet as remote control" trick: today's deck embed URL in a Sheet cell.

## Content embeds (analyzed; Croix wants this — kills tab-switching)
- iframe from file:// to https is allowed; the TARGET site decides if it displays; the school filter still applies inside frames.
- **Google Slides**: publish-to-web embed player works in an iframe (read-only viewer URL; authoring stays in Google → complement preserved). **CONFIRMED: the Publish-to-web dialog is available on Croix's LCS account** (screenshot Aug 2026). Remaining test: confirm the `/embed` URL renders on the classroom machine; check "Published content & settings" for restrict-to-domain (preferable for curriculum content; domain-restricted embeds still work on the signed-in machine). His decks contain inert Slides Timer syntax (`<<5:00->>`) — the OS supersedes it.
- Works: YouTube (filter permitting), PhET, GeoGebra published materials; Desmos worth testing. Refuses framing: non-published Google URLs, most login sites, Kahoot/Blooket game screens.
- Deck turnover: one persistent published "Daily Board" deck per class (edit, don't replace), or the Sheet-as-remote-control trick.
- Chrome **Document Picture-in-Picture** floating mini-timer over presented Slides: worth a 2-min test on the school machine; may be policy-disabled; degrade gracefully.

## Competitive reference
**ClassroomScreen** (classroomscreen.com) — 26-widget dashboard; free tier can't save screens; Pro $36/yr. Croix uses/knows it; Deckhand continues because it can't match his design family, bell-schedule awareness, offline/file:// operation, or local-only data. Its widget list = validated demand signal; its landing page inspired Deckhand's. Content-display widgets (PDF/video/embed beyond Slides) are Slides' job.

## Owner context that matters
- Croix maintains a house design language and cares that his tools look like a family. Coastal palette above is current; older materials: navy/gold/cream, DM Serif Display + DM Sans.
- Technical (React/Firebase, Python, Apps Script) — explain plainly, don't oversimplify.
- **Deployment reality (corrected Sep 2, 2026): the classroom machine is a CHROMEBOX (managed ChromeOS) driving the Promethean touch panel.** Deckhand lives in Croix's Google Drive and opens via ChromeOS DriveFS as a `file:///media/fuse/drivefs-…` URL — file:// deployment confirmed working live in class (photos, Sep 2). Consequences: **the noise meter cannot get a microphone there** (tested live: no prompt, immediate block — Chrome/device policy for local files; not fixable in-app, message says so honestly), and the download-copy loop goes through Drive (which doubles as his backup — nice). Live in front of 7th graders as of Sep 2. Stability rule: **each phase ships a complete file; his daily-driver copy is never the one being operated on.** Version + date in downloaded filenames.
- **Schedule updates: Croix photos the bell sheet to Claude, who reads it and bakes the config directly into the file** (done for 2026-27) — beats typing into textareas.
- Students returned Aug 10, 2026. School year is underway.

## Croix's school-machine checklist (v6)
1. Open the file → landing greets you; the week chip is pre-picked (rotation); Open the Daily Board. Home (H) returns.
2. Try the canvas on the touch panel: drag a widget by its title bar, resize by the corner, + Timer, switch to the Stations scene, and **Lock** before students touch it. Arrangements only survive via Settings → **Download configured copy** → replace the file.
3. Verify against real bells this week; if the countdown is off by seconds, Settings → nudge → download → replace.
4. Sim preview: append `#t=2026-09-02T10:30` (a Wednesday) → "Teal Week (Wed)" with Wednesday times; remove hash for real use (coral SIM tag = simulated).
5. Keep the current file + this handoff in Google Drive.

## Open items
- ~~Which week is Aug 31?~~ ANSWERED: Black Week — baked in as the rotation anchor; weeks now auto-pick.
- ~~Green light for Phase 3?~~ GIVEN — v6 shipped Aug 31, 2026; awaiting Croix's Promethean touch test (drag/resize feel, lock, scenes).
- Nudge calibration against the real bells (one-time). Students return Sep 10 per Croix ("we have time").
- ~~Classroom-machine test of the published-deck `/embed` URL~~ **EFFECTIVELY PASSED (Sep 2)**: Croix framed a deck from the probe file on the Chromebox — he pasted the `/edit` link rather than a published one, and it STILL rendered fully interactive inside the iframe (filter allows docs.google.com; framing works from file://). **The Slides embed card is greenlit.** Remaining nicety: have him run Publish-to-web once and confirm the clean `/embed` player (no editor chrome). Probe file: `Deckhand_Embed_Probe.html` (auto-converts /pub→/embed).
- Noise meter on the Chromebox: CLOSED as environmental — mic denied to file:// by policy, no prompt (tested live Sep 2). Widget message updated to say so. Works only where Chrome grants mic to local files (e.g., some home machines).
- Phase 4 green light + which widgets first (Slides embed card is the headliner; picker/group maker/scoreboard next; QR + sound meter LOW).
