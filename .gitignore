.github-token
node_modules/
tmp_*.html
tests/tmp_*.html
test-results/
playwright-report/
.playwright/
# an exported copy of Deckhand carries rosters — Chrome's duplicate-download
# names, and any "configured" copy, must never be added by accident
Deckhand (*).html
Deckhand_v6 (*).html
Deckhand*configured*.html
Deckhand*export*.html
.DS_Store
/tests/tmp_fx_*.html

# v7.8.1: the school-hosting mirror lives outside the public repo (see HANDOFF)
hosting/
